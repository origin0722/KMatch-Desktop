# 桌面体验与内容质量整改计划（2026-08-23）

> 来源：用户审阅意见（10 项）→ 已拆为 GitHub issues #61–#70，均为 vertical-slice（前后端/UI/测试穿到）。
> 执行顺序按依赖与风险排布；每项完成后跑相关测试，全部完成后前后端全量回归 + 严格复查。

## Issue 映射

| # | Issue | 类型 | 关键改动 |
|---|---|---|---|
| 61 | 导航栏透明化 + 可伸缩折叠 | UX | NavSidebar 折叠态 + sidebar store 持久化 + ResizablePanel 联动 |
| 62 | 启动就绪门 | UX | Workspace 前加 ReadyGate（轮询 backendHealth，超时卡片可重试/跳过；LLM 缺失非阻塞） |
| 63 | 设置收敛三大块 | Refactor | SettingsView 重组：AI 助手 / 学习引擎 / 联网搜索 / 通用；供应商 CRUD 内联 |
| 64 | 删冗余小字 + 顶部居中 | UX | AssistantPanel 空态清理 + 居中；全局装饰性小字清理 |
| 65 | 学习目标跳转丢失 | Bug | StageGoal 目标持久化到 store；返回时加载态展示、不重置 |
| 66 | 运行历史领域/场景/薄弱点 | Enhancement | 后端 persist 补 scene+weak_topics；RunsPanel 场景标签+薄弱点 chips |
| 67 | 题库可审计 + 生成深度 | Content | audit 脚本+pytest；反馈讲义 500-800 字、测试 3 题带解析 |
| 68 | search_weak_topics 工具 + 薄弱点 badge | Enhancement | 新工具注册/执行；Learning 联网资源薄弱点标识 |
| 69 | 时间估计修正 | Bug | _build_profile 周数 pacing 化；Dashboard 优先 learningReport.pacing |
| 70 | 知识图谱导读对话化 | Enhancement | 空态导读 chip + 图谱面板"图谱导读"预填 |

## 执行顺序

1. **#65 目标跳转 bug**（纯前端 store/StageGoal，影响面小，先修）
2. **#69 时间估计**（后端 _build_profile + Dashboard + persist）
3. **#66 运行历史**（后端 persist 字段 + RunsPanel UI）
4. **#68 weak 资源工具**（registry + chat 执行 + Learning badge）
5. **#70 图谱导读**（空态 chip + 详情面板按钮 + 预填 prompt）
6. **#64 小字清理 + 居中**（纯 UI）
7. **#61 导航折叠**（sidebar store + NavSidebar + Workspace 联动）
8. **#62 启动就绪门**（ReadyGate 组件 + backendHealth 复用）
9. **#63 设置收敛**（SettingsView 重组，复用现有子组件/store）
10. **#67 内容质量**（审计脚本 + pytest + content_generator prompt 升级）

## 验收（全部完成后）

- 前端全量 vitest 通过；后端全量 pytest 通过
- 逐 issue 对照验收标准复查（见最终复查报告）
- 变更提交并推送；按需发布新版本（另行确认）

---

# 执行状态：全部完成（2026-08-23）

| # | 状态 | 落实点 |
|---|---|---|
| 61 | ✅ | `sidebar.navCollapsed` 持久化 + NavSidebar 折叠按钮/图标轨 + Workspace 重挂载收窄 44-60px；玻璃感更透明（bg 62% + blur 14 + 弱化边框） |
| 62 | ✅ | 新增 `ReadyGate.vue`：轮询 backendHealth 1.5s，就绪自动进入；18s 超时 + 3s 失败卡（重试/跳过）；LLM 未配置改为引导文案（chat.js 友好化） |
| 63 | ✅ | 设置收敛为 AI 助手（含自定义厂商+视觉探测）/ 学习引擎 / 联网搜索（Tavily+网络代理）/ 通用 四大段；移除 API 设置与供应商管理独立段；锚点同步 |
| 64 | ✅ | 删除「基于 DeepSeek V4 Pro…」+ 4 条 feature 小字；空态 chips 与快捷按钮居中；KnowledgeGraph 装饰性小字移除 |
| 65 | ✅ | `pendingTargetDirection` 双向同步（StageGoal↔store）；返回不重置、恢复原目标；在途"正在准备题目"提示；失败内联错误+重试 |
| 66 | ✅ | 后端 persist 补 scene/weak_topics/pacing（demo+interactive）；RunsPanel 场景标签 + 薄弱点 chips + 详情 pacing/薄弱点 |
| 67 | ✅ | `scripts/audit_question_bank.py`（648 题/288 节点/6 基座域/完备率 100%）+ 5 条防回归 pytest；反馈讲义 500-800 字（代码示例/误区对照/表格）、实操 400-700 字、测试 3 道（基础/进阶/挑战 + 答案解析） |
| 68 | ✅ | 新工具 `search_weak_topics`（registry/执行/摘要/面板摘要）；Learning 联网资源「薄弱点」badge + 只看薄弱点筛选 |
| 69 | ✅ | `_build_profile` 周数按候选学时/6h 折算（封顶 8）；Dashboard 优先 `learningReport.pacing`；run summary 落 pacing |
| 70 | ✅ | `GRAPH_GUIDE_PROMPT` 双入口：AI 空态「🧭 知识图谱导读」chip + 图谱详情「图谱导读」按钮（预填→chat）；补 askAi 单测 |

## 最终回归

- 前端 **63 文件 / 456 测试全过**（新增：ready-gate 2、question-bank 5、导读 1、薄弱点工具 2、场景/薄弱点 2、目标持久化 2、导航折叠 1）
- 后端 **683 通过 / 2 skip**（新增 5 条题库审计 + 名称结转/画像 name 等）

## 严格复查（对照验收标准）

- **#61** 折叠态仅图标 + tooltip、状态持久化、主区自动变宽 ✅；拖拽调宽仍可用（双 panel-key 分离）✅
- **#62** 就绪前不渲染主界面（ReadyGate 前置）✅；超时错误卡可重试/跳过 ✅；无 LLM key 仍可进入 + AI 引导文案 ✅
- **#63** 仅 3 配置主段 + 通用 ✅；自定义厂商在两段可直接选择（厂商池复用）✅；联网搜索段含 Tavily + 连通性（代理保留）✅；相关设置测试全绿 ✅
- **#64** 空态无「基于 DeepSeek…/feature 列表」✅；顶部快捷/chips 居中 ✅；前端 456 全过 ✅
- **#65** 自定义目标→开始→切走→返回：目标不变 + 加载态可见 + 完成自动出题 ✅；失败显示错误+重试 ✅（新增）
- **#66** 每条历史含场景/领域/薄弱点 ✅；旧记录无新字段优雅降级（chips 无则显示 —）✅
- **#67** audit 脚本可跑 + pytest 防回归 ✅；生成 prompt 深度升级（500-800 字/3 题带解析）✅；后端全量 ✅
- **#68** 工具注册/权限/示例/提示词齐全 ✅；AI 调用后落 Learning（addFeedbackLinks）✅；薄弱点 badge + 筛选 ✅
- **#69** 周数不再随弱项数膨胀（按学时/每周 6h，封顶 8 周）✅；Dashboard 与 pacing 一致 ✅
- **#70** 空态 chip 一键发起导读 ✅；图谱面板预填并切 chat ✅；回复基于真实路径/节点（工具回喂已修复）✅

## 遗留说明（非阻塞）

- 反馈生成质量（深度/专业性）依赖 LLM 实际输出，本次以 prompt 契约 + 审计 + 回归测试保证；上线后可用 `scripts/run_judge_golden.py` 抽查。
- `ApiSettings.vue` 保留文件但不再挂载（其"统一"能力由两段独立配置替代）；后续如需"一键统一"可再做。

