# 深度检查报告 — 代码漏洞 / 赛题规范 / 项目完整度（2026-08-19）

> 范围：全仓三轴审计（后端安全与正确性、赛题 XH-202630 需求追溯、交付完整度）。
> 方法：源码走查 + 调用点核实 + 实测（后端 660→667 测(+2 skip) 基线级回归）。
> 结论：无阻断性缺陷；本报告含 5 项已修复 + 2 项遗留加固建议（已开 issue 留档 #59/#60）。

---

## 一、代码漏洞（安全 / 正确性 / 并发）

| # | 严重度 | 发现 | 证据 | 状态 |
|---|---|---|---|---|
| 1 | **高危** | 嵌入式项目图落盘路径穿越：`EmbeddedGraphStore._project_path` 直接拼接 API 自由字符串 `project_id` → 可越界写任意路径（`data/local/projects/<pid>.json`） | `backend/app/graph/embedded.py` + `api/project.py` ParseRequest 无 pattern | ✅ 已修：白名单正则 `^[A-Za-z0-9_-]{1,64}$`（embedded 层）+ Pydantic pattern + GET 路由显式校验；回归 `tests/test_project_security.py` |
| 2 | 中危 | 示例项目加载路径未校验：`load_example_project(name)` 直接 `EXAMPLE_PROJECTS_DIR / name`，多段/绝对路径可读任意目录下 `.py` | `backend/app/code_parser/loader.py` | ✅ 已修：拒绝多段/`..`/绝对路径 + `is_relative_to` 双重护栏 + 4 组参数回归 |
| 3 | 中危 | KB 题目查找非递归：`kb_store.find_question` 只扫顶层，嵌套域题目（DA/DB/WD…）CRUD 漏查/漏删；嵌入式已递归、Neo4j 导入已递归 → 两后端不一致 | `backend/app/data/kb_store.py` + import 脚本 rglob | ✅ 已修：`glob("**/*.json")`；注：`save_question` 仍写顶层，嵌套域新建题目存在同 qid 双文件风险（低概率，已记录） |
| 4 | 低 | 嵌入式向量缓存合并无锁：自动回填与单节点回填并发时读改写丢条目 | `embedded._write_embeddings` | ✅ 已修：`_vector_lock` 互斥 |
| 5 | 低 | `/api/agents/ping` 异常 `str(exc)` 可空 → 前端只能显示"未知错误" | 上轮修复（v1.0.1，#57/#58） | ✅ 已修 |
| 6 | 低 | Electron 渲染层加固项：`sandbox:false` + 无 CSP meta；`contextIsolation:true + nodeIntegration:false` 已达标 | `electron/main/window.js` | ⚠️ 建议（issue #60）：评估 `sandbox:true` 与 CSP |
| 7 | 低 | 打包环境内代码测试沙箱不可用（无 Python 运行时，`sys.executable` 为 exe）——既有已知限制（`KMatchBackend.spec`） | spec 注释 + 冒烟 | ⚠️ 已知边界（issue #59）：缓解措施待评估 |

**核查通过项**：uvicorn 仅绑 127.0.0.1（issue-45）；CORS 白名单本机；沙箱环境变量白名单（不传密钥）；用户代码 AST 预检 + `write_file` 审批门；per-request `llm_overrides` ContextVar 在 with 块内 set/reset 无泄漏；嵌入式全部写路径原子写（临时文件 + `os.replace`）；项目图写入幂等。

## 二、赛题规范 XH-202630 追溯矩阵

| 赛题要求摘要 | 实现落点 | 验证状态 |
|---|---|---|
| 场景覆盖 + 不同背景学习者适配 | 6 域知识库 + 场景一/二 + 10 组差异化画像 | 单测 + 画像文件 |
| ≥3 个职责分工智能体，"分析-生成-校验-决策"闭环 | 7 个 Agent（orchestrator 编排）+ reviewer/judge 双校验 + quality_judge 独立裁判 | 契约测试 + 工作流测试 + golden 回归 |
| 整合先验画像 + 领域知识库 | 10 画像 + 222 节点 + 题库抽题与 LLM 补题 | 测评链路 |
| 动态生成 ≥3 种形态资源 | 分层讲义 / 实操指南 / 分阶测试题（content_generator + 报告组件） | 报告单测 |
| 可视化（盲区定位 / 难度匹配曲线 / 学习路径规划图） | PathFinder / LearningReport / KnowledgeGraph / FlowDiagram | 前端组件测试 |
| 反馈动态迭代（降维解释 / 进阶挑战） | `decide_feedback` 策略（advance/remediate/scaffold） | 单测 |
| 交叉验证抗幻觉（创新性） | judge 独立裁判双口径 + evidence 锚定 + `test_judge_goldens` 12 例 | 全量回归 |
| 数据合规 / 隐私 | 本地存储、构建产物白名单排除运行痕迹（profile_archive 等） | 安装包审计（v1.0.1 已核） |
| 提交材料：单元测试用例（协同调度 + 生成准确性核心模块） | 后端 667 测(+2 skip) + 前端 434 测（含诊断/生成/裁判专项） | 全量通过 |
| 测试数据：≥1 垂直领域切片 + ≥2 组画像 | 6 域 222 节点 + 93 题库；10 组画像 + 完整输入输出样例（根目录画像 + run 资产） | 数据文件核查 |
| M5 三指标（幻觉<5% / 适配≥85% / 覆盖≥90%） | quality_metrics 双口径 + 质量检测报告 + 独立裁判 | 报告 + golden |
| 可执行程序 + 部署说明 | v1.0.1 安装包（NSIS）+ 真机核验清单 | 已分发（待实机） |
| 演示视频（10 分钟闭环） | 材料大纲/视频脚本已备 | ⚠️ 待录制（人工） |
| 代码测试（场景二）在安装包环境 | 开发/评委环境用 docker 或 Python dev 全栈可用 | ⚠️ 安装包内受限（issue #59） |

## 三、项目完整度

| 项 | 现状 | 处理 |
|---|---|---|
| Spec B Task 18-19 代理落盘 | **已接线**（`ipc/proxy.js` set/get + 落 userData + sidecar spawn 注入 proxyEnv; issue #49 已关闭） | ✅ 速查卡"待做"标记已修正为已完成 |
| 文档漂移（CLAUDE 速查卡） | 节点/题库/画像数量、ActivityBar 旧架构引用 | ✅ 已同步（222 节点/8 提示词/10 画像/NavSidebar） |
| `.env.example` 缺新变量 | GRAPH_STORE / KMATCH_DATA_DIR / KMATCH_LOCAL_DIR 未记录 | ✅ 已补 |
| docker-compose `apoc` 未使用 | 未 grep 到调用 | ⚠️ 建议清理（低优先） |
| release 旧 blockmap 残留 | `KMatch·知链-0.1.0-x64.exe.blockmap`（gitignore 内） | ⚠️ 建议清理（低优先） |
| 首启/实机全流程 | 核验清单已备（v1.0.1） | ⚠️ 待真机执行（人工） |

## 四、本次已修复清单（随本提交）

1. 嵌入式 `project_id` 路径穿越（白名单 + API pattern + GET 校验）
2. `load_example_project` 多段路径拒绝
3. `kb_store.find_question` 递归（嵌套域题目 CRUD 可达）
4. 嵌入式向量合并并发锁
5. 文档漂移（Spec B 状态、CLAUDE 数据量、.env.example）

## 五、遗留建议（已开 issue — 已全部落地于 v1.0.2）

- ~~#59：安装包内代码测试沙箱不可用~~ ✅ **已落地**：`scripts/build_portable_python.ps1` 构建便携 Python 运行时（pytest 9.1.1 + pytest-cov）随 backend-dist 分发 → 安装包内 `SubprocessSandboxExecutor` 优先解析 `resources/backend/runtime-python/python.exe`，场景二代码测试端用户可用。
- ~~#60：Electron 渲染层加固~~ ✅ **已落地**：`sandbox:true`（preload 仅用 electron 的 contextBridge/ipcRenderer，兼容 sandboxed preload）+ 渲染层 CSP meta（`frontend/index.html`；script-src 预留 unsafe-eval 供 Vue 模板编译，收紧至仅本源；connect-src 放行本机后端与 dev HMR）。

## 六、验证

- 新增 `backend/tests/test_project_security.py`（7 用例：parse pattern 422 / GET 404+400 / loader 4 组拒绝 / 嵌入式路径白名单）+ `tests/test_sandbox_python_resolution.py`（4 用例：dev/frozen 解析 + cmd[0] 走便携运行时）。
- 后端全量 **671 过 + 2 跳**、前端 434 过。
- 打包验证：v1.0.2 安装包（145MB）已含 `resources/backend/runtime-python/python.exe`（pytest 9.1.1 实测可用），data 白名单保持纯净；sidecar exe 手工冒烟 15/15 健康 + 节点/路径端点通过。
- 遗留人工项：真机安装→首启→全流程回归（核验清单）与 10 分钟演示视频录制。