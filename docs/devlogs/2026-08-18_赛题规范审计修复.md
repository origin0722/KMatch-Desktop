# 2026-08-18 赛题规范审计 + 首批修复

> 对全仓做了一次系统性代码审计（后端 Agent 层 / API+图谱引擎 / 前端 store+视图 / Electron+部署，4 路并行 + 个人复核 + 双端测试实测）。
> 审计结论与详细逐条对齐见会话报告；这里记录落档与修复。

## 一、测试实测（2026-08-18）

- 后端 pytest（全量访问实跑）：**551 / 551 通过** ✅
- 前端 vitest：352/355（3 失败均与本批改动无关，属本机高负载 flaky——1 个 5s 硬超时 + 2 个静态核对与代码一致，建议 CI 复跑确认）

## 二、落档（GitHub issue）

按项目约定"新 bug 一律开 issue"，共开 12 个：

- [#38 Learning.vue 本地即时判分功能完全无响应（testStates 非响应式）] bug/critical
- [#39 Learning.vue 填空/代码题答案解析失败 → 全判"正确"+题干泄答案] bug/major
- [#40 Dashboard M5 三指标键失配：后端真实质量指标永不显示] bug/major·赛题锚点
- [#41 幻觉定向再生下标坐标系错位 → 可能改错资源] bug/major
- [#42 interactive 链路 learningReport 未填充 + /api/learning/report 未接线 + 学情报告三联未渲染] bug/major·赛题锚点
- [#43 activeStage 在 feedback 阶段过早跳 graph] bug/major
- [#44 SSE 测评缺浏览器回退 + chat 流断连无兜底] bug/major
- [#45 无鉴权开放 LLM/搜索代理 + key 内存滞留（赛题(5) 数据合规）] bug/major·赛题锚点
- [#46 代码测试沙箱健壮性：--cov 选项注入 / exit 2 视为成功 / baseline 超时过短] bug/minor
- [#47 Electron fs 写入守卫：符号链接父目录可写穿工作区] bug/minor
- [#48 部署符合性：docker-compose 服务缺 Dockerfile 无法构建 + setup_docker_mirror 脚本缺失] bug/major·赛题锚点
- [#49 Spec B Task 18-19：网络代理主进程落盘未接线（跟踪）] enhancement·赛题锚点

## 三、本批修复（已提交）

| Issue | 修复内容 | 改动文件 |
|---|---|---|
| #38 | 判分状态 `reactive()` 化（普通 Map+普通对象不触发重渲染） | `frontend/src/views/Learning.vue` |
| #39 | 答案正则支持非字母填空/代码答案；题干截断到 `**答案**` 前；门控 `?.parsed`；填空精确归一比对（去 includes 子串误判） | `frontend/src/views/Learning.vue` |
| #40 | Dashboard 改读后端真实键 `*_rate?.rate`（`source:'backend'` 分支复活）；测试 mock 对齐真实契约 | `frontend/src/views/Dashboard.vue`、`frontend/src/__tests__/assessment-store.test.js` |
| #41 | judge 不再预过滤（`resource_index` 保持原始列表坐标），与 regen 的 `out` 索引对齐；补"前位空资源"回归单测 | `backend/app/agents/quality_judge.py`、`backend/tests/test_quality_judge.py` |
| #43 | `activeStage` 在 phase=answering/feedback 期间留在 quiz，不因 hasResults 过早跳 graph；补 2 个回归用例 | `frontend/src/stores/session.js`、`frontend/src/__tests__/session-store.test.js` |
| #44 | `startAssessmentStream` 补浏览器 fetch 回退（/snippet 分帧 + 60s 看门狗）；chat 浏览器回退 90s 看门狗；IPC 中止统一 AbortError 语义（与浏览器一致，工具循环不再把"已停止"当完成）；`sendMessage` 闸改 isBusy | `frontend/src/api/diagnostics.js`、`frontend/src/ide/chat/useChatStream.js`、`frontend/src/stores/chat.js` |

另随本批带上工作区已有的两处图谱画布自适应（ResizeObserver，`KnowledgeGraph.vue` / `ProjectGraphView.vue`，评审确认无 bug、生命周期干净）。

## 四、验证

- 后端质量链路定向回归：`pytest tests/test_quality_judge.py tests/test_quality_regen.py tests/test_quality_metrics.py` → **47 通过**（含新增 #41 回归用例）
- 前端改动文件 node 语法级全过（含 SFC script 抽取）；判分解析 4 用例 node 实测通过（选择/小写答案/填空 -1/填空语句）
- 前端全量 vitest 因沙箱限制未能在本环境复跑（此前 352/355，3 失败与本批无关）

## 五、遗留 → 已全部在本日第二批闭环（见下）

- #42 interactive 打通 `/api/learning/report` + 学情报告三联渲染（较大特性）
- #45 鉴权/开放代理收敛（赛题(5) 安全）
- #46 沙箱健壮性、#47 fs 符号链接父目录穿越
- #48 Dockerfile 补齐 + mirror 脚本、#49 Spec B 18-19 代理落盘

---

# 第二批修复（同日晚，闭环剩余 6 个 issue）

## 修复清单

| Issue | 修复 | 改动 |
|---|---|---|
| #42 | interactive 链路接通 `/api/learning/report`：新增 `api/learning.js` + store `loadLearningReport()`（幂等/失败兜底/会话失效降级）+ Dashboard 进页自动补跑；M5 指标可显示"后端真实计算"；修 learningReport JSDoc 为真实契约键（blind_spots/difficulty_match/learning_path/review_status/quality_metrics；三联本已由 Dashboard 客户端等效渲染，补齐后端数据源） | `frontend/src/api/learning.js`(新）、`stores/assessment.js`、`views/Dashboard.vue` |
| #45 | 安全收敛：dev/打包默认只绑 127.0.0.1（`KMATCH_HOST` 可覆盖供容器）；`_validate_base_url` http/https scheme 白名单（/completions·/models·/probe-vision 非法→400，不写 vision 缓存）；LLM client 缓存键改 api_key sha256（原始 key 不再驻留 dict 键） | `backend/app/main.py`、`run_server.py`、`api/chat.py` + 测试 |
| #46 | 沙箱健壮性：cov_module 白名单 + `--cov` 分离参数（防 pytest 选项注入）；pytest exit code 2（收集错误）不再当"成功启动"，单独报错；baseline 模式保底 20s 预算防小套件误判超时；补 `_pytest_cov_args` 纯函数单测 | `backend/app/agents/sandbox.py`、`code_tester.py`、`tests/test_sandbox_unit.py` |
| #47 | Electron fs 写守卫：目标不存在(新建)时改为逐级上溯最近存在祖先并 realpath 校验，堵"父目录为指向外部符号链接"写穿工作区；补单测 | `electron/main/ipc/fs.js`、`frontend/src/__tests__/fs-write-guard.test.js` |
| #48 | 部署符合性：新增 `backend/Dockerfile`、`frontend/Dockerfile.dev`；`scripts/setup_docker_mirror.ps1/.sh`（配置 registry-mirrors + 重启指引）；vite 代理目标支持 `VITE_PROXY_TARGET`（容器内→backend:8000）；compose 前端 env 由无效的 VITE_API_BASE_URL 改为 VITE_PROXY_TARGET | `backend/Dockerfile`、`frontend/Dockerfile.dev`、`frontend/vite.config.js`、`docker-compose.yml`、`scripts/setup_docker_mirror.*` |
| #49 | Spec B 18-19 代理落盘接线：`ipc/proxy.js`（userData/proxy.json 原子写 + env 构造，NO_PROXY 排除本地回环）+ sidecar spawn env 注入 + `restartBackend()` + preload `setProxyConfig/getProxyConfig/restartBackend` + main 注册 | `electron/main/ipc/proxy.js`(新)、`backend-sidecar.js`、`index.js`、`preload/index.js`、设置页注释 |

## 验证（本机）

- `pytest tests/test_quality_judge.py tests/test_quality_regen.py tests/test_quality_metrics.py tests/test_chat_models_api.py tests/test_chat_build_request_extras.py tests/test_chat_anthropic_stream.py tests/test_chat_openai_to_anthropic_msg.py tests/test_chat_safety_api.py + 新 cov-args 用例` → **83 / 83 通过**
- `docker compose config` → 通过（compose YAML/引用合法）；Dockerfile/依赖文件（requirements.txt、package-lock.json）就位
- 后端改动模块 ast+import 校验通过；前端改动（assessment.js / api/learning.js / fs-write-guard.test.js / Dashboard 脚本 / vite.config / electron 4 文件）node --check 全过
- 全量 vitest 仍受沙箱限制未复跑（同前批说明）

