# 2026-08-18 — Phase 2：流程即数据（流程定义 + preflight + 溯源）

**会话目标**：把"协同执行"从写死的链提升为**可命名、可校验、可溯源、可替换**的流程资产——
借鉴 dsh_workflow capsule 思想，落地其最小子集。Phase 3 画布的数据底座。

## 一、产出

| 产出 | 文件 | 说明 |
|:---|:---|:---|
| 流程定义模块 | `backend/app/agents/workflow_def.py`（新） | `kmatch.workflow` v1 schema：manifest(id/name/description) + stages(id/label/agents/dependencies) + inputs；内置 3 个默认流程（scene1-loop / scene1-interactive / scene2-project）+ `data/workflows/*.json` 追加扩展（内置不可被遮蔽，坏文件跳过不致命） |
| 校验 + preflight | 同上 | `validate_definition`（必填/未知字段/未知 Agent/依赖乱序/自依赖/重复 id）；`preflight`（定义合法 + 输入契约 + max_retries/scene 边界）——坏定义/坏请求**启动前被拒** |
| 运行前门卫 | `backend/app/api/diagnostics.py` | demo `/assess` 与 SSE 前 `_preflight_or_400`；SSE 进度文案改由**流程定义 stage.label 驱动**（改定义即可改展示，不改代码） |
| 溯源 | `backend/app/agents/run_store.py` | run.json 增加 `workflow` 快照 + request.workflow_id（复盘可知当时跑的拓扑） |
| 流程 API | `backend/app/api/diagnostics.py` | `GET /workflows`（列表）/ `GET /workflows/{id}`（详情）/ `POST /workflows/preflight`（干跑） |
| 前端 | `frontend/src/api/diagnostics.js` | `fetchWorkflows()`（Phase 3 画布消费） |
| 后端单测 | `backend/tests/test_workflow_def.py`（新） | 14 例：校验规则/discovery/preflight/API |

## 二、测试

- 后端：`test_workflow_def`(14) + `test_run_store` + `test_assess_sse_api` + `test_log_events` = 39 passed
- 前端：diagnostics-api +4 例；全量 374/374
- 排障记录：先期 5 个用例失败根因是**测试浅拷贝污染模块级 BUILTIN_WORKFLOWS**（`stages` 列表共享引用被 `defn["stages"][0]=...` 改坏）→ 测试改深拷贝；产品代码未改动

## 三、待办（Phase 3-4）

- Phase 3：G6 流程画布（只读进度 DAG → 可编辑拓扑 + 事务审查）——以 `fetchWorkflows` + 定义数据为底座
- Phase 4：逻辑门自适应分支（确定性布尔求值）
