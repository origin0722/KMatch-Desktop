# 2026-08-18 — 画像 diff 落 run 复盘 · 遗忘/时效降级 · 03 embedding 降级条款

**会话目标**：落地"先做低成本高确定性"一组——④ diff 落复盘联动、② 遗忘/时效降级、⑦ 03 提示词降级显式条款，同步文档。

## 一、④ 画像 diff 落 run 复盘（P1 × 画像档案联动）

- `submit` 落盘 run：summary 增加 `profile_diff` + `learner_key` → `GET /runs/{id}` / `loadRun` 复盘时可见画像版本变化。
- **顺带修掉一个潜伏 bug**：interactive 的 `_persist_run` 之前写在 `return SubmitResponse` **之后**（死代码），interactive run **从未落盘**——P1 埋下、无测试覆盖；本轮新测试抓出并修复（`_persist_run` 移到 return 前）。demo 路径不受影响。
- 前端 `loadRun` 从 `run.summary.profile_diff` 回灌 `profileDiff` → 重进会话/复盘同样能看到"本次变化"。

## 二、② 遗忘/时效降级（`profile_store`）

- 实测掌握度条目带 `last_test_at`；结转的 known 超 `STALE_AFTER_DAYS`(30) 未重测 → 标 `recheck_due`（待重验）。
- 前端答题反馈「画像进化」卡新增：`⚠️ n 个知识点已超 30 天未重测`，提示复习/重测。
- 纯函数 `merge_profiles(prev, new, now=)` 支持注入时间（可测）。

## 三、⑦ 03 embedding 降级显式条款

- `03_graph_controller_agent.txt` 注意事项新增：`semantic_search`/`hybrid_retrieve` 的向量路依赖 embedding；未配置时**自动降级为仅图遍历**（`_source` 恒 "graph"），调用方不得假定语义分数可用。
- `test_prompt_contract` +1：钉 03 必须含"降级/semantic_search/hybrid_retrieve/图遍历"。

## 四、测试

- 后端：profile_store(+时效/recheck_due)、contract(+03 条款)、submit(shutdown run 断言)等 = **88 passed**
- 前端：`loadRun` 回灌 profileDiff(+1) 等 = **411/411**

## 五、后续可续（按影响/成本）

- ⑥ 裁判 golden 回归集（护 M5 指标）→ ① 实操/风格落地 与 ③ 动态域并入主画像（大块）。
