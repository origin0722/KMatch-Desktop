# 2026-09-02 — P0 项目图谱可信度与测试基线

**参与成员**: 自己  
**会话目标**: 完成项目图谱可信度 P0：明确能力边界、修复单文件质量检查入口、规范历史展示与删除、消除 Agent 状态误导，并恢复全量测试的环境独立性。

## 一、产出

| 产出 | 文件 | 说明 |
|:---|:---|:---|
| 当前文件质量检查 | `frontend/src/views/ProjectGraphView.vue` | 修复未定义画像引用；入口改名并明确仅支持 Python，调用 Code Review → Code Test → 修复建议的只读流水线。 |
| 运行历史展示契约 | `backend/app/agents/run_store.py`、`frontend/src/ide/RunsPanel.vue` | 列表只读取安全的展示投影，按学习/项目场景输出稳定标题与上下文。 |
| 单项历史删除 | `frontend/src/views/KnowledgeGraph.vue`、`frontend/src/views/ProjectGraphView.vue` | 可删除单个本地图谱快照，明确不删除当前图谱、项目源文件或学习画像。 |
| Agent 延后启动状态 | `frontend/src/composables/useAgentStatus.js`、`frontend/src/components/StageAgent.vue` | 资源生成与内容审核在前置流程完成、但尚未进入资源阶段时显示“生成资源后启动”，不再误导为卡死。 |
| 测试环境隔离 | `backend/tests/test_diagnostics_unit.py`、`backend/tests/test_llm.py`、`backend/tests/test_submit_api.py` | 测试显式注入 LLM 假实现与配置状态，不依赖开发机 `.env` 或真实网络。 |

## 二、验证

- `pytest backend/tests -q`：778 passed，2 skipped。
- `npx vitest run --reporter=dot`：70 个测试文件、545 个测试通过。
- `npm run build`：成功完成生产构建。
- 已知非阻断提示：VueUse 注释和 bundle 体积提示，以及部分 jsdom 组件/Canvas 警告；均无测试失败。

## 三、能力边界

- 当前“项目代码图谱”解析能力仍限 Python（AST/Jedi）。
- Java/Spring Boot 的首个语义图谱垂直切片，以及 TypeScript、JavaScript、Rust、C 的适配器属于后续 P1/P2；本次不将文档计划写成已实现能力。
- 项目语义图谱的证据、置信度、框架识别与协同解释设计见 `docs/superpowers/specs/2026-09-02-project-semantic-graph-design.md`。

## 四、待办

- [ ] 实现 Java/Spring Boot 的结构事实提取与框架语义投影。
- [ ] 将问卷逻辑改为按学习方向动态分支，并将画像独立为设置页。
- [ ] 将历史记录收敛为可管理的统一工件中心。
