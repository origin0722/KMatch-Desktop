# 2026-08-18 — 后台任务页（运行历史）+ Markdown Mermaid 图表

**会话目标**（用户体验第一，用户点单 ①② 做 / ③ 不做）：① 后台任务页——复用 P1 耐久 run + 协同事件；② Markdown 预览支持 Mermaid 图表。

## 一、后台任务页（RunsPanel）

- `frontend/src/ide/RunsPanel.vue`（新）：运行历史列表（模式 tag / 目标 / 统计 chips / 📈 画像变化标记）→ 点行展开**事件时间线**（Agent 状态带色：done/failed/degraded/running + 消息，上限 40 条）→ 操作：demo「按此重跑」（`loadRun`→`resumeRunDemo` 续跑）、任意「重新测评该目标」（interactive→`startAssessment` / demo→`startDemoStream`），随后切学习会话。
- 接线：sidebar `ACTIVITY_ITEMS` 增「运行历史」(id=runs, icon=Clock)；MainArea 增 `RunsPanel` 分支（异步懒加载 + no-pad）。
- 数据源：`GET /api/diagnostics/runs`（list）+ `GET /runs/{id}`（详情）——P1 资产直接复用，无新后端。
- 测试：`runs-panel.test.js` 5 例（列表/事件展开/demo 重跑/interactive 重测/失败提示）。

## 二、Markdown 预览支持 Mermaid

- 新增依赖 `mermaid@^11`（npm install 64 包 19s，已入 package.json/lock）。
- `FilePreview.vue`：marked 自定义 renderer——` ```mermaid ` 代码块 → 占位 div(`data-mmd`)；DOMPurify 净化后（content 渲染完成 `loading=false`）再 `await import('mermaid')` 惰性渲染 SVG（`startOnLoad:false`）；**任一环节失败 → 降级为源码块 + 提示，不崩**。
- 关键修复：mermaid 后处理必须放在 `loading=false` 之后（此前在 loading=true 时跑，mdBox 未挂载 → 空扫）。
- 测试：`file-preview` +1（mock mermaid.render 抛错 → 断言降级源码块+提示）。

## 三、测试

- 前端全量 **434/434**（后台任务页 5 + mermaid 1；既有 428 全保留）。后端无改动。

## 四、用户点单外不做

内嵌浏览器 / Git 面板 / Office 预览 / 真实终端（③ 不做）——已固化进"不做"清单，避免范围蔓延。

## 五、后续可续（若有）

- 后台任务页：加"正在运行"实时态 + 退出码/时长列；Mermaid 成功路径补真机目检。
- 懒加载/预览/任务页三件已让二次开发文件工作流（打开快 + 看图看文看图 + 历史可回放）整体顺滑。
