# 2026-08-14 IDE 化 UI 收官（剩余工作 V2）

> 承接 UI 改造方案 V2（评审定案）。V1 五阶段（NavSidebar / view-card / 引导卡片向导 / 图谱 PERSONA+间距）此前已落地 main，本次完成剩余 5 任务，5 个独立提交。

## 背景

plan.md V1 与代码现状对照后发现 70% 已实现；本次收窄为剩余工作，三项决策经确认：

1. **AI 助手双形态** - 主区 `chat` 视图（760px 居中式对话）+ 右侧可折叠分栏共存，chat store 单会话双形态共享（已入 CONTEXT.md）。
2. **图谱详情真 split** - flex 推挤画布替代浮层遮盖，删 panelGap 避让矛盾。
3. **引导接受卡片向导** - 不改对话式，只做 UX 打磨。

## 提交清单

| 提交 | 任务 | 内容 |
|---|---|---|
| 94155bb | T1 | 删死代码 ActivityBar.vue + activitybar-theme.test.js；项目速查卡 索引换 NavSidebar |
| 6dc7917 | T2 | 设置页「通用」段重新引导入口；`onboardingActive` 收口 sidebar store（免刷新重触发）；settings-view 测试更新为 4 锚点 + 新增接线用例 |
| 9df5781 | T3 | 图谱侧栏浮层->split 分栏占位；删 initGraph panelGap=300 避让；路径摘要+节点详情融合为「图谱详情」单卡；折叠切换延迟 220ms 重建（等宽度过渡结束） |
| 21b7e90 | T4 | AI 助手双形态：ACTIVITY_ITEMS 加 `chat` 项（学习会话之后）；MainArea 装载 `variant="wide"`（760px 居中 + 建议 chip 胶囊）；Workspace 在 chat 视图下隐藏右侧侧栏（避免双挂载）；NavSidebar 底部开关更名「AI 分屏」 |
| 0626bd8 | T5 | 引导走完自动落 chat 视图（跳过保持 code，done 事件带 skipped payload）；Key 过短软提示不阻断；就绪步文案对齐 |

## 关键实现点

- **AssistantPanel variant 纯样式层**：`defineProps({ variant })` + 根类 `.wide` CSS 覆盖，对话逻辑（chunks 判别联合/工具卡/版本切换/审批门/SSE/附件）零改动。
- **chat 视图进 no-pad 列表**：view-card 透明容器 + `max-width:760px; margin:0 auto`，无大卡片边框。
- **双挂载规避**：`v-if="sidebar.aiPanelVisible && sidebar.activeView !== 'chat'"`——chat 视图本身即对话，切回其他视图侧栏即恢复。
- **el-input 测试 stub 升级**：v-model 双向（props+emits），否则 setValue 不触发更新。

## 测试

41 文件 / 278 测试全过（较开始 +2：重新引导接线、Key 软提示；activitybar-theme 随死代码移除）。chat-attachments 偶发超时已证实为全量并发下 flaky（单独跑 7/7 过）。

## 已知待办

- [ ] 逐视图手测 + 截图对比（尤其 chat 视图与 Monaco v-show 切换）
- [ ] `npm run dev` 里清 `kmatch-onboarded` 验收引导全流程（含 T2 设置页重新触发）
