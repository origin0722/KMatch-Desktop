# 2026-08-18 — Phase 1：耐久 run 记录（复盘/续跑）

**会话目标**：phase 0 的结构化事件是"看得见"，phase 1 要"留得住"——把每次 demo 全流程 / interactive submit 的执行记录落盘为可复盘、可续跑的 run 资产。延续工作流插件借鉴方案（5 阶段）的第 2 阶段。

## 一、产出

| 产出 | 文件 | 说明 |
|:---|:---|:---|
| run 存储模块 | `backend/app/agents/run_store.py`（新） | `save_run`（run.json 原子写 + events.jsonl append-only 续写 seq）/ `load_run` / `list_runs`；session_id 去安全化防路径穿越；DATA_DIR 可配 |
| 落盘接线 | `backend/app/api/diagnostics.py` | demo `/assess`、SSE done、interactive `/submit` 三处 `_persist_run`（含请求 meta + 汇总）；失败不影响主流程 |
| 读取 API | `backend/app/api/diagnostics.py` | `GET /api/diagnostics/runs/{session_id}`（复盘回灌）/ `GET /api/diagnostics/runs`（历史列表） |
| 前端 API | `frontend/src/api/diagnostics.js` | `fetchRun(sessionId)` / `fetchRuns(limit)` |
| store 复盘/续跑 | `frontend/src/stores/assessment.js` | `loadRun(sid)`（404→null，回灌事件/日志+lastRun）；`resumeRunDemo()`（读 lastRun.request 一键重跑）；`lastRun` state |
| 续跑入口 | `frontend/src/components/session/StageAgent.vue` | 上次有 demo run 时显示「↻ 按此流程重新执行」 |
| 运行数据 | 新增 `.gitignore`：`data/workflow_runs/` | run 记录本地生成，不进库 |
| 后端单测 | `backend/tests/test_run_store.py`（新） | 9 例：回环/续写 seq/路径穿越/原子写/排序/GET 404/列表 |
| SSE 测试加固 | `backend/tests/test_assess_sse_api.py` | Data_DIR 指向临时目录，防测试写真实 run 文件 |
| 前端单测 | `frontend/src/__tests__/assessment-run-store.test.js`（新） | 6 例：loadRun 回灌/404/非404上抛/startDemoStream 记 meta/resume 重跑/非 demo 防空跑 |

## 二、测试

- 后端：`test_run_store` + `test_assess_sse_api` + `test_log_events` = 26 passed
- 前端：新增 6 + 受影响套件全绿；全量 368 passed，2 个失败（chat-attachments、workspace-watcher）为并行满载下 3s 级慢用例的偶发超时，**隔离单跑 14/14 通过**，与本次改动无共享依赖（非回归）

## 三、待办（Phase 2-4）

- Phase 2：学习流程即数据（capsule v1 子集：manifest/intent/inputs + preflight）——链数据化
- Phase 3：G6 流程画布（只读进度 DAG → 可编辑拓扑 + 事务审查）
- Phase 4：逻辑门自适应分支（确定性布尔求值）
