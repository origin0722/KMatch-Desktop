# 2026-09-02 — AI 助手 UX 第一批修复 (体验审查落地 1-5 项)

**参与成员**: 自己
**会话目标**: 落地 AI 助手体验审查的第一批（赛前低风险）修复。审查发现见会话记录, 第二批 (diff 审批/流式渲染节流/多会话等) 赛后再做。

## 一、修复清单

| # | 问题 | 修复 |
|:--|:---|:---|
| 1 | **中文输入法选词回车误发送** — onKeydown 只判 `Enter && !shiftKey`, 未查 `isComposing`, 半截拼音被直接发出 (全中文产品几乎人人会撞) | `onKeydown` 首行加 `if (e.isComposing \|\| e.keyCode === 229) return` (`AssistantPanel.vue`) |
| 2 | **停止生成被回传为"用户拒绝写入"** — 流中点停止会把未决 write_file 审批按拒绝解开, 语义错位 | `stopStreaming` 改 `_cancelPendingApproval({ approved: false, stopped: true })`; write_file 分支按 `decision.stopped` 区分措辞: "用户停止了本次生成（未写入文件，非拒绝写入）" |
| 3 | **生成期间输入框整体禁用** — 等回复时连草稿都不能打; 模型/思考模式切换也被锁 | textarea 仅在后端不可用时禁用; 发送按钮保持 isBusy 禁用; `handleSend` 守卫从 `streaming` 收紧为 `isBusy` (**忙时静默保留草稿**, 不再出现"输入被清空但没发出去"); 厂商/模型/思考模式切换放开 (只影响下一次请求, 在途流不受影响); 导学开关保持锁定 (改在途回合的系统提示词语义) |
| 4 | **用户消息零操作** — 不能复制、不能编辑重发 | 用户消息 hover 操作: 复制 + 编辑重发 (编辑态 textarea 替换气泡; 保存后更新文本、**截断其后对话**、重走助手回合; 带附件消息只换 text 段保留附件)。store 新增 `editUserMessage` + 抽出 `_runAssistantTurn` (sendMessage/editUserMessage 共用工具循环) |
| 5 | **中断无标记 + 文案不一致** — 部分输出后停止与正常完成不可区分; 截断提示写「重试」但按钮叫「重新生成」且 opacity:0 | AbortError 分支: 有部分内容时置 `msg._stopped` 角标 (**不改写正文**, 复制/重发不带杂质), UI 渲染"⏹ 已停止"; 空输出仍走"(已停止)"文本兜底; 文案统一为「重新生成」; regen 按钮基础 opacity 0.45 常显 (hover 全显) |

## 二、验证

- 新增 `chat-ux-fixes.test.js` 6 用例: 编辑重发 (文本替换+截断+新回合历史正确 / 附件保留 / isBusy no-op)、停止审批带 stopped 标记、部分输出停止 `_stopped` 角标且正文无"(已停止)"、空输出停止文本兜底。
- 前端全量: **71 文件 / 555 测试全过** (+6); `npm run build` 生产构建成功。

## 三、待办 (第二批, 赛后)

- [ ] write_file 审批改 Monaco diff 视图 (整文件 textarea → 原新对照)
- [ ] 流式渲染节流 (delta 全量 marked+DOMPurify+v-html 重建 → 节流批量, 代码块展开状态保住)
- [ ] 上下文 48k 裁剪可感知 (系统 pill 提示"较早 N 条未随请求发送")
- [ ] 多会话管理 / @ 引用文件 / Monaco 选区问 AI / SSE 断线重连 / 后台完成未读提醒
- [ ] 流式 stats 挂到消息 (目前 lastStats 单值, 翻旧消息看不到)
