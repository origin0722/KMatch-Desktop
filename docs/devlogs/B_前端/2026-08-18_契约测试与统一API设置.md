# 2026-08-18 — 契约漂移测试 + 统一 API 设置

**会话目标**：①把"提示词—代码"双源契约钉死，防漂移；②在设置页新增「API 设置」栏目——
AI 助手与出题/Agent 引擎的 API 可**统一或分开**配置，带**预设模型**与**测试连通性**按钮。

## 一、契约漂移测试（Q2-A）

| 文件 | 说明 |
|:---|:---|
| `backend/tests/test_prompt_contract.py`（新, 6 例） | 钉死: orchestrator 打回≤3轮 & LLM 重试≤2次 & 分档(≥0.8/0.5) ↔ decide_feedback；审核阈值 0.85 ↔ REVIEW_PASS_THRESHOLD；code_safety 高危清单(eval/exec/os.system/pickle) ↔ 07 一票否决；04 幻觉治理条款(高保真/先锚定/unverified_claims/心算)；08 动态建域(10节点/不纳入M5)；事件词汇 ↔ log_events.AGENT_KEYS |

## 二、统一 API 设置（设置页「API 设置」栏目）

| 产出 | 文件 | 说明 |
|:---|:---|:---|
| 协调层 store | `frontend/src/stores/apiSettings.js`（新） | `mode`(unified/separate) + `unified` 一份配置 + `applyUnified()` 落 aiSettings+agentLlm 两端 + `testConnectivity()`(POST /api/agents/ping) + 预设模型 `MODEL_PRESETS`(deepseek/openai/anthropic/moonshot/qwen/glm/gemini/ollama) |
| 栏目 UI | `frontend/src/ide/settings/ApiSettings.vue`（新） | 统一开关；统一模式: 厂商/BaseURL/模型(datalist 预设可手输)/Key + 「应用到全部」+「测试连通性」；分开模式: ①AI 助手 ②出题/Agent 引擎 两行各自编辑+测试 |
| 设置页接入 | `frontend/src/ide/settings/SettingsView.vue` | 新增 `sec-api` 栏目（置顶，锚点「API 设置」） |
| 单测 | `apiSettings-store.test.js`（5）+ `api-settings.test.js`（3）+ `settings-view.test.js`（锚点数量/定位改健壮） | 统一落两端/换厂商自动带 baseUrl/presets/连通性走 ping |

> 设计说明：这是 **协调层**，不做新的单一真相源——AI 助手仍读 `aiSettings`、Agent 引擎仍读 `agentLlm`（chat.js/withOverrides 不变）；「统一」即把一份配置写到两端，「分开」即两端各自独立。连通性复用既有 `POST /api/agents/ping`（openai 兼容）。

## 三、测试

- 后端：`test_prompt_contract` 6 + 既有 = **58 passed**
- 前端：全量 **405/405**（新增 8）

## 四、待办/后续

- 「阶段测试题直接给答案」定位：**分阶测试题(Learning 测试题 tab)** 的生成内容自带 **答案/解析**，UI 还有「直接看答案」按钮——建议改为「先提交自测、再对照」；阶段测评本身答题不显答案、错题回顾为折叠（已核实）。
- 流程工作台按反馈为非必需：可选隐藏侧栏入口（保留代码与数据）。
