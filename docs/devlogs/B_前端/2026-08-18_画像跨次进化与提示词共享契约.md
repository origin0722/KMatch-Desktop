# 2026-08-18 — 画像跨次进化档案 + 提示词共享契约页

**会话目标**（按建议组合落地）：① 把"每次测评都重建新画像、无历史"的短板升级为**跨次累积/进化档案**（含版本 diff）；② 并行补**提示词共享契约页**作为全局单一来源。

## 一、画像跨次累积/进化（`profile_store`）

| 产出 | 文件 | 说明 |
|:---|:---|:---|
| 画像档案库 | `backend/app/agents/profile_store.py`（新） | `merge_profiles`(纯函数：近次优先平滑 0.6/0.4 + 跨类边界以本次实测为准 → recovered/regressed 可达) + `save_profile`/`load_profile`(data/profile_archive，gitignore) + learner key 防穿越 |
| submit 接线 | `backend/app/api/diagnostics.py` | `SubmitRequest.learner_key`；提交时加载历史画像→合并→落库→复用 profile_id→返回 `profile_diff`(recovered/newly_known/newly_weak/regressed) |
| 前端 | `stores/assessment.js` + `api/diagnostics.js` + ``StageQuiz.vue`` | 稳定 `learnerKey`(localStorage 一次生成)；提交透传 `learner_key`；答题反馈展示「📈 画像进化 · 本次变化」 |
| v1 取舍（文档化） | — | 合并只覆盖 known/weak 掌握度真相源；weakness_areas/recommended_path/等级仍以本次为准；未重测旧节点原样结转（遗忘/衰减留待后续） |

## 二、提示词共享契约页（`data/prompts/00_shared_contracts.md`）

- 新页：JSON 严格性、溯源标记、日志/事件词汇(agent-start/agent-end/degraded)、阈值与常量(0.85/3/2/0.8 分档)、与 workflow_def/scene 对齐、质量护栏(幻觉治理/安全硬规则/建域/裁判解耦)。
- 01-08 主链提示词头部统一引用该页（单一来源，防"文件 vs 代码/各提示词各自写"漂移）。
- `test_prompt_contract` 新增 2 例：钉共享页与代码常量一致 + 校验 01-08 均引用。

## 三、测试

- 后端：`test_profile_store`(5: 首次/进化加权/carry/diff/save·sanitize + submit 集成) + `test_prompt_contract`(+2) + submit/diagnostics 等 = **77 passed**
- 前端：全量 **409/409**（diagnostics-api +1 learner_key 断言）

## 四、后续可续

- 画像：遗忘/时效降级、动态域并入主画像、画像版本 diff 落到 run 复盘页。
- 提示词：内联提示词(08/05)抽文件、judge golden 回归集、03 无 embedding 降级显式条款。
