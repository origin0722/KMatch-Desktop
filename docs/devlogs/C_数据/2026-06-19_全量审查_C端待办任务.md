# C 端（数据与质量负责人）全量审查 & 待办任务清单

> 审查日期: 2026-06-19 | 审查人: C | 代码状态: `8e3993a` (W7②质量检测)
> 范围: 259 个文件全量审查 | 原则: 只审查不动代码

---

## 一、代码库演进概况（自 6/17）

| 维度 | 6/17 状态 | 6/19 状态 |
|:---|:---|:---|
| Agent 节点 | 2 个 (diagnostics+reviewer) | **7 个** (全就绪) |
| API 路由 | 1 个 (diagnostics) | **4 个** (diagnostics+graph+learning+project) |
| 代码解析器 | 无 | **5 文件** (AST+Jedi+Loader+Models) |
| 测试文件 | 3 个 | **20+ 个** (306+ 用例) |
| BUG 记录 | 16 条 | **44 条** (全解决) |
| 知识节点 common_mistakes | 无 | **92 节点全填充** |
| 独立题目库 questions/ | 无 | **92 文件 276 题** |
| 质量检测体系 | 无 | **3指标批量脚本+报告** |
| Prompts | 6 个 (v0.1-draft/v0.2-align) | **7 个** (依然旧版本标签) |

---

## 二、C 端待办任务（按优先级）

### 🔴 P0 — 阻塞性/规范不一致

#### 任务 1: 全量 Prompt 审查与版本升级

**现状**：7 个 prompt 文件全部标注 C 为负责人，但 A 在 W2-W7 开发过程中持续修改了 prompt 内容（补字段、加约束、对齐实现），版本标签未跟随更新。

| 文件 | 版本标签 | A 实际更新日期 | 代码实现状态 |
|:---|:---|:---|:---|
| `01_orchestrator_agent.txt` | v0.2-align | 6/19 | 全字段已实现 |
| `02_diagnostics_agent.txt` | v0.2-align | 6/19 | 题库驱动出题已对接 |
| `03_graph_controller_agent.txt` | **v0.1-draft** | 6/19 | W6 已全部实现 |
| `04_content_generator_agent.txt` | **v0.1-draft** | 6/19 | W4 已实现+层次1减幻觉 |
| `05_content_reviewer_agent.txt` | **v0.1-draft** | 6/19 | 双模式均已实现(BUG-016解决) |
| `06_code_tester_agent.txt` | **v0.1-draft** | 6/19 | W6 已实现 |
| `07_code_reviewer_agent.txt` | **v0.1** | 6/19 (新建) | W6 已实现 |

**要求**：
1. C 逐文件对比 prompt 文本与代码实际行为，确认无偏差
2. 将 03/04/05/06 从 `v0.1-draft` 升级到 `v0.3-final`（或 `v1.0`）
3. 将 01/02 从 `v0.2-align` 升级到 `v0.3-final`
4. 将 07 从 `v0.1` 审查后升级到 `v0.3-final`
5. 更新每个文件头部的"更新"记录
6. 【可并行】此任务完成后更新 CLAUDE.md L78

**影响**：prompt 版本标签误差达 3 周，协作者无法判断 prompt 可信度。

---

#### 任务 2: validate_data.py `key_points` 最少数量与 Schema 不一致

**位置**：[backend/scripts/validate_data.py:94](backend/scripts/validate_data.py#L94)

```python
# validator (宽松)
if not isinstance(key_points, list) or len(key_points) < 2:
    errors.append("key_points 至少需要 2 条")
```

```json
// schema.json (严格)
"key_points": { "minItems": 3 }
```

**验证**：当前 92 个节点全部满足 ≥3 条，数据本身无问题，但校验门禁比 Schema 低一档。若未来有节点仅 2 条 key_points，会通过验证但违反 Schema。

**要求**：将 `len(key_points) < 2` 改为 `len(key_points) < 3`，与 schema.json 完全对齐。（此文件在 A 目录，但 data quality 是 C 职责，需协调 A 修改或经沟通后自行修改。）

---

### 🟡 P1 — 文档同步

#### 任务 3: CLAUDE.md 过期信息更新

**3 处过期**：

| 行号 | 当前内容 | 应为 |
|:---|:---|:---|
| L78 | "6个 Agent 系统提示词初稿" | **7** 个（新增 07_code_reviewer_agent.txt） |
| L140 | "BUG 清单: 41 条 (41 已解决)" | **44** 条（BUG-001~044） |
| L106-110 | 开发阶段仅到 W6 | 补充 **W7 质量检测** 状态 |
| L97 | LLM 对话: `deepseek-v4-pro` | ✅ 已正确（config.py 同步） |

**要求**：更新以上条目。参考 BUG决策日志中 BUG-042~044 的 W7② 进展。

---

#### 任务 4: 前置数据准备指南.md 同步

**需补充的新内容**：

1. **`common_mistakes` 字段** — 92 节点全部填充（schema.json 已更新 minItems=1），需在指南中说明：字段语义、生成方式（`generate_common_mistakes.py` 脚本）、5 处运行时消费点
2. **`questions/` 独立题目目录** — 92 文件 × 每节点 3 题 = 276 题，字段包括 `qid`、`source_node_id`、`explanation`、`node_id` 等
3. **第 7 个 Agent Prompt** — `07_code_reviewer_agent.txt`（场景二·代码审查）
4. **M5 质量检测指标体系** — 幻觉率<5%、适配率≥85%、覆盖率≥90%，`run_quality_test.py` 批量脚本

---

#### 任务 5: 项目开发计划书.md W7 状态同步

**需更新**：开发排期表中 W7 的完成状态、M5 质量检测里程碑达成情况、赛题对标表。

---

### 🟢 P2 — 增强与优化

#### 任务 6: questions/ 目录添加独立 Schema

**现状**：题目格式靠 `validate_question()` 函数隐式校验（已覆盖 qid/source_node_id/type/answer/difficulty），但无 JSON Schema 文件供协作者参考。

**要求**：编写 `data/knowledge_base/questions/schema.json`，定义标准题目格式，供：
- 协作者理解题目结构
- IDE 自动补全
- `validate_data.py` 引用

---

#### 任务 7: M5 质量检测报告审查

**现状**：`data/quality_report.json` 显示 3 画像全部达标（幻觉率 0%、适配率 100%、覆盖率 100%），由 `run_quality_test.py` 生成。

**要求**：
1. 审查 `quality_metrics.py` 的指标计算方式是否与赛题 M5 要求对齐
2. 确认阈值（幻觉<5%、适配≥85%、覆盖≥90%）设置合理
3. 确认 `docs/质量检测报告.md` 已生成（人读交付物）

---

#### 任务 8: C 端开发日志补充

**现状**：A 端有 6/18~6/19 共 ~15 篇日志，B 端有 ~4 篇。C 端仅有 6/16 初始化日志和 6/17 代码审查报告。

**要求**：补充以下日志：
- `2026-06-18_数据层同步状态.md` — 记录 common_mistakes 填充、questions 目录建立、prompt 更新追踪
- `2026-06-19_W7全量审查与任务规划.md` — 本文件即为主要交付物

---

#### 任务 9: 边界测试数据准备

**要求**：为 `questions/` 和 `user_profiles/` 准备边界/异常测试数据：
- 格式异常题目（缺 qid、非数组）
- 交叉矛盾画像（known 与 weak 重叠）
- 空画像
- 供 `validate_data.py` 和单元测试使用

---

## 三、已确认无需处理项 ✅

| 项目 | 状态 |
|:---|:---|
| 92 节点全部通过 validate_data | ✅ |
| 4 个画像全部通过双阶段校验 | ✅ |
| 276 道题目全部通过结构校验 | ✅ |
| common_mistakes 92 节点全部填充 | ✅ |
| .env.example LLM_MODEL 已同步 deepseek-v4-pro | ✅ (6/19 已更新) |
| json_utils.py 解决 _parse_json 重复代码 | ✅ (6/18 已解决) |
| BUG-016 reviewer 双模式已实现 | ✅ (6/18 已解决) |
| BUG-025 recommended_path 字段统一 | ✅ (6/18 已解决) |
| BUG-043 content_generator 难度赋值 | ✅ (6/19 已解决) |
| docker-compose.yml Embedding 环境变量已补 | ✅ |
| 07_code_reviewer_agent.txt 已添加 | ✅ (6/19) |

---

## 四、C 端 W7 剩余工作量估计

| 任务 | 预估耗时 | 优先级 |
|:---|:---|:---|
| 任务 1: Prompt 审查+版本升级 (7文件) | 2-3h | 🔴 P0 |
| 任务 2: key_points 校验对齐 (协调A) | 15min | 🔴 P0 |
| 任务 3: CLAUDE.md 更新 | 20min | 🟡 P1 |
| 任务 4: 前置数据准备指南同步 | 1h | 🟡 P1 |
| 任务 5: 项目计划书同步 | 30min | 🟡 P1 |
| 任务 6: questions schema 编写 | 30min | 🟢 P2 |
| 任务 7: 质量指标审查 | 30min | 🟢 P2 |
| 任务 8: 开发日志补写 | 30min | 🟢 P2 |
| 任务 9: 边界测试数据 | 1h | 🟢 P2 |
| **合计** | **~7h** | |

---

> **审查结论**: 代码库质量从 6/17 到 6/19 有质的飞跃（7 Agent 全就绪 + 代码解析器 + M5 质量体系）。C 端主要欠账在 **文档同步延迟**（Prompt 版本标签/CLAUDE.md/指南/计划书），均为非代码类文档工作，不影响系统运行但影响协作效率。P0 两任务应在今天完成。
>
> —— C，2026-06-19
