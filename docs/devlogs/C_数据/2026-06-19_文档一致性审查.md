# C 端 — 文档与代码一致性审查报告

> 审查日期: 2026-06-19 | 审查人: C | 审查范围: 全部文档 vs 实际代码
> 原则: 不动代码，只记录不一致供修正

---

## 发现汇总

| # | 严重度 | 文档 | 位置 | 问题 |
|:---|:---|:---|:---|:---|
| 1 | 🔴 | CLAUDE.md | L34 | "编排6个子Agent" → 应为 **7个** |
| 2 | 🔴 | CLAUDE.md | L68 | "6/6 Agent 节点" → 应为 **7/7** |
| 3 | 🔴 | CLAUDE.md | L78, L116 | "6个 Agent 系统提示词初稿" → 应为 **7个** (两处重复) |
| 4 | 🔴 | CLAUDE.md | L140 | "BUG 清单: 41 条" → 应为 **44 条** (BUG-001~044) |
| 5 | 🔴 | 项目开发计划书 | L568 | M5 里程碑状态 "🔄 进行中" → 应为 **✅ 已完成** (quality_report.json 三指标全达标) |
| 6 | 🟡 | 前置数据准备指南 | 全文 | 遗漏 `common_mistakes` 字段说明 (92节点已填充、schema已更新) |
| 7 | 🟡 | 前置数据准备指南 | 全文 | 遗漏 `questions/` 独立题目目录 (92文件×3题=276题) |
| 8 | 🟡 | 前置数据准备指南 | §三 | "Agent 系统提示词初稿" 仍写 6个 → 应为 **7个** |
| 9 | 🟡 | 前置数据准备指南 | §五 | Cypher CREATE_NODE 示例漏 `common_mistakes` 字段 |
| 10 | 🟢 | BUG决策日志 | BUG-031~044 | 格式不一致 (`###` vs `##`)，建议统一为 `##` |

---

## 逐项详析

### 🔴 #1·2·3: CLAUDE.md Agent/Prompt 数量错误

**CLAUDE.md 三处声称 6 个 Agent，但实际代码实现了 7 个**：

| 行号 | 当前文本 | 应修正为 |
|:---|:---|:---|
| L34 | `主控调度Agent → 编排6个子Agent` | 编排 **7** 个子Agent |
| L68 | `6/6 Agent 节点全部实现` | **7/7** Agent 节点全部实现 |
| L78 | `6个 Agent 系统提示词初稿` | **7个** Agent 系统提示词 |
| L116 | `6个 Agent 系统提示词初稿` | **7个** Agent 系统提示词 |

**根因**: 第 7 个 Agent (`code_reviewer`) 在 W6 追加，`07_code_reviewer_agent.txt` 在 6/19 才创建，文档计数未同步更新。

**代码事实**:
- 7 个 LangGraph Agent 节点: orchestrator, diagnostics, reviewer, graph_controller, content_generator, code_tester, **code_reviewer**
- 7 个 Prompt 文件: 01~07 全部 `v0.3-final`
- code_reviewer 文件: [backend/app/agents/code_reviewer.py](backend/app/agents/code_reviewer.py) ✅ 已存在

---

### 🔴 #4: CLAUDE.md BUG 清单数量错误

| 行号 | 当前文本 | 事实 |
|:---|:---|:---|
| L140 | `BUG 清单: 41 条 (41 已解决)` | BUG决策日志实际有 **44 条** (BUG-001~044，全部已解决) |

**根因**: BUG-042~044 (W7②质量检测修复) 追加后 CLAUDE.md 未更新计数。最新三条为：
- BUG-042: assemble_learning_path 弱项漏覆盖 (覆盖率 33%→100%)
- BUG-043: content_generator LLM 自填 difficulty_level (适配率 52%→100%)
- BUG-044: feedback 再生路径漏修 BUG-043

---

### 🔴 #5: 项目开发计划书 M5 里程碑状态过期

[项目开发计划书.md:568](项目开发计划书.md#L568):
```
| M5 质量检测达标 | 第7周末 | ... | C+A | 🔄 进行中（质量检测指标接口由A端实现中） |
```

**代码事实**: 
- `quality_metrics.py` — 已实现 ✅
- `run_quality_test.py` — 已实现，可批量跑 ✅
- `data/quality_report.json` — 三指标全达标 (幻觉率0%/适配率100%/覆盖率100%) ✅
- `docs/质量检测报告.md` — 已生成 ✅

**应改为**: `✅ 已完成（三指标全达标：幻觉率0%/适配率100%/覆盖率100%）`

---

### 🟡 #6·7·8·9: 前置数据准备指南 四大遗漏

**遗漏 1 — `common_mistakes` 字段**:
- Schema 已于 6/18 更新 `minItems: 1`
- 92 节点全部填充（每节点 3-4 条常见误区）
- `validate_data.py` 已将缺失 `common_mistakes` 列为错误
- 但指南中节点字段表 (L44-56) 和示例节点 (L230/L281) 均无此字段

**遗漏 2 — `questions/` 独立题目目录**:
- `data/knowledge_base/questions/` 含 92 个题目文件
- 每文件 3 题，共 276 题，含 `qid`/`source_node_id`/`explanation` 字段
- `validate_data.py` 已有阶段 1.5 题目校验（`validate_questions_dir`）
- 指南 §一"领域元知识库" 无此说明

**遗漏 3 — Prompt 数量**:
- §三 "Agent 系统提示词初稿" 仍提及 6 个，但已有 7 个

**遗漏 4 — Cypher 示例过时**:
- §五 `CREATE_NODE` 示例 (L384) `practice_questions=$practice_questions` 未包含 `common_mistakes`，与 `import_knowledge_base.py` 实际逻辑不一致

---

### 🟢 #10: BUG决策日志格式不一致

BUG-001~030 使用 `## BUG-XXX: 标题`（二级标题），BUG-031~044 使用 `### BUG-XXX: 标题`（三级标题，嵌套在批次章节下）。建议统一为 `##` 以保持编号连续性和 grep 友好。

---

## 需协调 A/B 的项

| # | 问题 | 涉及方 | 说明 |
|:---|:---|:---|:---|
| #1-4 | CLAUDE.md Agent/BUG计数 | A（CLAUDE.md维护者） | C 可自行修改（共享文件），与 A 知会即可 |
| #5 | 计划书 M5 状态 | C+A | C 更新文档，A 确认质量指标实现无误 |
| #6-9 | 指南缺失字段 | C（指南负责人） | C 全权负责 |
| #10 | BUG日志格式 | 共享 | 格式美化，不急 |

---

> **审查结论**: 文档整体质量良好，但 **CLAUDE.md 有 4 处硬错误**（6→7 Agent、41→44 BUG），**项目计划书 M5 里程碑状态过期**，**前置数据准备指南缺少 common_mistakes/questions 两个关键数据资产**。均为代码正确、文档滞后——不影响系统运行但误导协作者。
>
> —— C，2026-06-19
