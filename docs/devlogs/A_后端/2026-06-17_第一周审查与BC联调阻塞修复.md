# 2026-06-17 — 第一周工作审查 + 修复 B/C 联调阻塞

**参与成员**: A（后端）
**会话目标**: 对照计划书审查第一周工作完成度与对 B/C 的影响，修复联调阻塞项
**耗时**: 半日

---

## 一、第一周工作审查结论

对照计划书 A 第1周四项任务逐项核查：

| # | 任务 | 状态 | 证据 |
|:--|:---|:---:|:---|
| ① | 环境搭建（Python/Node/Neo4j/Docker） | ✅ | langchain1.3.9/neo4j6.2.0/fastapi0.137.1/openai2.41.1/langgraph 均已安装；docker-compose 三服务编排完整 |
| ② | LangGraph 最小多Agent Demo | ✅ | `week1_demos/langgraph_demo/main.py` 实跑通过：generate→review→条件分支→3轮打回循环→finish |
| ③ | 大模型API + Neo4j Cypher 读写联调 | ✅ | `week1_demos/neo4j_demo/main.py` 三段式代码完整；.env 已配真实 DeepSeek+千问 key |
| ④ | **FastAPI + LangGraph 集成方案验证** | ⚠️ | main.py 中 "langgraph" 仅出现在 version 接口字符串返回值，**无真正 import langgraph**。LangGraph 仅在独立 demo 验证，未接入 FastAPI —— 此项实为第2周开局工作 |

**结论**：核心目标基本达成，④ 名不副实（留待第2周补）。超额交付了 engine.py 图谱引擎骨架（本属第3周）和画像 v3 统一。

## 二、发现的对 B/C 影响及修复（BUG-012）

审查发现 3 个会拖累 B/C 联调的问题，本会话全部修复：

| 影响 | 问题 | 修复 |
|:---|:---|:---|
| 🔴 | docker-compose.yml 漏配 `EMBEDDING_*` → B/C 一键启动时语义检索静默降级（DeepSeek 无 embedding API） | backend service 补 5 个 `EMBEDDING_*` 环境变量，默认对接千问 DashScope |
| 🟡 | `.env.example`/`config.py` 默认 `text-embedding-3-small`，与实际千问 `text-embedding-v2` 不一致 | 两处默认值改为 v2，.env.example BASE_URL 填上千问 |
| 🟡 | week1_demos 模拟节点用 v1 字段 `title`/`node_id`，与正式 schema v3 的 `name`/`id` 脱节 | 两个 demo 字段全部对齐 v3 |

详见 [docs/缺陷管理/BUG决策日志.md](../../缺陷管理/BUG决策日志.md) BUG-012。

## 三、产出

| 产出 | 文件 | 说明 |
|:---|:---|:---|
| Docker 编排补全 | `docker-compose.yml` | backend service 补 EMBEDDING_* 环境变量 |
| 默认值统一 | `backend/app/config.py`、`.env.example` | EMBEDDING_MODEL 默认 → text-embedding-v2 |
| Demo 字段对齐 | `week1_demos/langgraph_demo/main.py`、`week1_demos/neo4j_demo/main.py` | title→name、node_id→id |
| BUG 记录 | `docs/缺陷管理/BUG决策日志.md` | 新增 BUG-012 + 汇总表 |

## 四、验证

```bash
# demo 字段无 v1 残留
grep -rn "node_id\|\.title" week1_demos/   # No matches found
# langgraph demo 实跑通过
python week1_demos/langgraph_demo/main.py  # ✅ 2节点+条件分支+3轮循环
# config 默认值正确
python -c "from app.config import settings; print(settings.EMBEDDING_MODEL)"
# → text-embedding-v2
```

## 五、遇到的 BUG

| BUG 编号 | 简述 | 解决状态 |
|:---|:---|:---|
| BUG-012 | Docker漏配Embedding+默认模型名不一致+demos用v1字段 | ✅ 已解决 |

## 六、环境变更

- 共享文件 `docker-compose.yml`、`.env.example` 有改动 → push 前已在团队同步，提醒 B/C pull 后留意（理论上无并发改动，不冲突）

## 七、待办

- [ ] 第2周：全局 State 定义（解锁 C 编写学情检测 Prompt）
- [ ] 第2周：学情检测 Agent + 内容审核 Agent + 主控调度 Agent
- [ ] 第2周：main.py 真正集成 LangGraph（补第一周遗留 ④）
- [ ] 第2周：开 `/api/diagnostics` 路由，Postman 跑通"学情检测→审核"局部流程
- [ ] 第2周：解决 BUG-011（lifespan + KnowledgeGraph 单例）、BUG-006（空画像边界）
