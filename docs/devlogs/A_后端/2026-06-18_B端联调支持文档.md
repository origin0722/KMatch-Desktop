# 2026-06-18 — B 端联调支持文档（feature 分支，不动 B 代码）

**参与成员**: A（后端）
**分支**: `feature/B-联调支持`
**会话目标**: 在不动 B 端代码前提下，为 B 提供 BUG-027~030 修复指引 + 联调用例集
**耗时**: 0.3 日

---

## 一、背景

B 端遗留 BUG-027~030（W3 审查发现，4 条均纯前端逻辑/UI）。A 受目录归属约束不能改 B 代码。用户问"不动 B 代码怎么帮 B"，A 评估后认为：4 条里 3 条（027/029/030）纯前端无法后端侧触及，唯一有后端配合空间的 028 改动性价比低。

故采用**给 B 弹药**而非改 B 代码的方式：提供精确到行号的修复补丁 + 真实响应样本用例集，B 照着改即可，不用读后端源码、不用配 Neo4j+LLM。

---

## 二、产出

| 文档 | 内容 |
|:---|:---|
| `docs/B端修复与联调指引.md` | BUG-027~030 逐条修复补丁（行号+可直接 copy 代码）+ W5 新接口（submit/feedback）封装指引 + 字段契约速查 + 降级场景对照 |
| `docs/B端联调用例集.md` | assess/submit/feedback/graph 各接口的成功/降级/错误响应 JSON 样本，可作 mock 或 Postman 验证 |

---

## 三、核实工作

修复指引的行号和代码片段全部基于当前 main 真实代码核实，非抄审查报告：
- `Assessment.vue:98-113` 确认 el-alert `#default` 插槽覆盖 description
- `stores/assessment.js:59-64,120-141` 确认 hasResults 空对象守卫 + 未检测空画像
- `AssessmentReport.vue:152,58` 确认 `?? ''` 使 `?? '（未作答）'` 成死代码
- `stores/assessment.js:129-133` 确认未映射 knowledge_graph/generated_content
- `api/diagnostics.js` 发现 B 端**尚未封装 W5 的 submit/feedback 接口**（审查报告未提及，本次补上）

---

## 四、对 B 的价值

1. **零成本修复**：4 个 BUG 照指引 copy 补丁即可，不用自己定位根因
2. **免环境开发**：用例集可 mock，B 不起 Neo4j+LLM 也能开发前端
3. **W5 接口对接**：submit/feedback 封装代码已写好，B 直接加进 api/diagnostics.js
4. **防踩坑**：字段契约速查标注了 common_mistakes 未填充、/path 返回键是 nodes 等易错点

---

## 五、A 端配合约定

文档明确：B 修复过程中若发现需后端配合（改字段名/降级语义），告诉 A，A 在后端侧调整；B 端代码仍由 B 自己改。当前 4 BUG 均纯前端，A 侧无需改动。

---

## 六、遗留

- 拦截器 404 走 else 显示"网络错误"语义不准（指引中已建议 B 补 404 分支，非 BUG 单列）
- common_mistakes 字段 C 端数据未填充（指引已提醒 B 兜底）
- 文档合并 main 后需通知 B 拉取
