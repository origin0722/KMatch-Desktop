# KMatch·知链 —— 前置数据准备指南

> **文档用途**：指导团队在开工前完成所有数据素材的准备，确保开发阶段不因数据缺失而卡壳
> **协调人**：C（数据与质量负责人）
> **参与成员**：A（后端）+ B（前端）+ C（数据）
> **建议完成时间**：2026 年 6 月 30 日前（开工前完成）

---

## 目录

- [一、领域元知识库](#一领域元知识库)
- [二、差异化用户画像](#二差异化用户画像)
- [三、Agent 系统提示词（7个，全部 v0.3-final）](#三agent-系统提示词7个全部-v03-final)
- [四、示例项目代码](#四示例项目代码)
- [五、代码规范与导入脚本](#五代码规范与导入脚本)
- [附录：数据质量检查清单](#附录数据质量检查清单)

---

## 团队数据分工一览

开工前5日，三人并行完成以下数据准备工作：

| 成员 | 数据任务 | 预估值 | 说明 |
|:---:|---|---|:---:|
| **A** | Python元知识库30个节点 + Neo4j导入脚本 + 2个示例项目 + 2个Agent Prompt | 2日 | 后端人最懂依赖关系和代码结构 |
| **B** | Python元知识库35个节点 + 3组用户画像原始数据收集 + 2个Agent Prompt | 2日 | 前端人接触真实用户，画像更准确 |
| **C** | Python元知识库27个节点 + 机器学习元知识库50个节点 + 格式规范统筹 + Prompt初稿 + 数据汇总验证 | 2—3日 | 质量负责人把控数据规范性 |

> **数据汇总节点**：开工前2日，C同学收集A、B提交的数据，运行 `validate_data.py` 验证完整性，修复格式问题后批量导入Neo4j。

---

## 一、领域元知识库

### 1.1 什么是领域元知识库

领域元知识库是系统的**知识底座**，是所有个性化知识图谱的"积木原料"。它不是由大模型生成的，而是由人工提前整理、经审核后导入 Neo4j 的静态知识库。

**个性化图谱不是"生成"的，而是从元知识库中"组装"的。**

### 1.2 数据模型定义

每个知识点（Node）包含以下字段：

| 字段                   |    类型    | 必填  | 说明                              | 示例                                          |
| :------------------- | :------: | :-: | :------------------------------ | ------------------------------------------- |
| `id`                 |  string  |  ✅  | 唯一标识，用领域缩写+编号                   | `PY-001`                                    |
| `name`               |  string  |  ✅  | 知识点名称                           | `变量与数据类型`                                   |
| `difficulty`         |   int    |  ✅  | 难度等级 1-5（1最易，5最难）               | `1`                                         |
| `category`           |  string  |  ✅  | 分类标签                            | `基础语法`                                      |
| `summary`            |  string  |  ✅  | 一句话概述                           | `Python中变量的定义规则与常见数据类型（int/float/str/bool）` |
| `prerequisites`      | string[] |  ✅  | 前置知识点ID列表，无前置则为空数组              | `[]`                                        |
| `key_points`         | string[] |  ✅  | 核心讲解要点（3-5条）                    | `["变量无需声明类型", "动态类型语言特性", "常见类型转换"]`        |
| `practice_questions` | object[] |  ✅  | 练习题（至少1道），题型支持 choice/fill/code | `[{ "type": "choice", ... }]`               |
| `common_mistakes`   | string[] |  ✅  | 常见误区列表（至少1条），用于 test干扰项/error_patterns/code审查 | `["认为变量必须先声明类型再赋值", "认为int有位上限"]` |
| `tags`               | string[] |  否  | 标签                              | `["基础", "入门"]`                              |
| `estimated_minutes`  |   int    |  否  | 预估学习时长（分钟）                      | `20`                                        |

> **v3 更新**（2026-06-19）：新增 `common_mistakes` 必填字段（schema.json minItems=1），5处运行时Agent消费（content_generator test干扰项/diagnostics error_patterns/code_tester/code_reviewer/reviewer 事实边界）。92节点已全部填充，`generate_common_mistakes.py` 脚本支持LLM批量生成。

### 1.3 练习题数据模型

每个知识节点内嵌 `practice_questions` 作为存档/校验用。**运行时题目由独立的 `questions/` 题目库供给**——学情检测Agent从 Neo4j `:Question` 节点（经 `:HAS_QUESTION` 关联）抽题，不依赖节点内嵌题目。

#### 节点内嵌练习题（`practice_questions`，存档用）

```json
{
  "type": "choice | fill | code",
  "question": "题目描述",
  "options": ["A. xxx", "B. xxx"],   // 仅 choice 题型
  "answer": "正确答案",
  "difficulty": 1,                    // 本题难度 1-5
  "hint": "提示信息（可选）"
}
```

#### 独立题目库（`questions/`，运行时用）

目录 `data/knowledge_base/questions/` 含 92 个题目文件（每节点 3 题，共 276 题），格式如下：

```json
[
  {
    "qid": "Q-PY001-001",
    "source_node_id": "PY-001",
    "node_id": "PY-001",
    "type": "choice",
    "question": "题目描述",
    "options": ["A. ...", "B. ..."],
    "answer": "C",
    "difficulty": 1,
    "hint": "提示",
    "explanation": "完整解释（含干扰项分析，对齐层次1减幻觉）"
  }
]
```

**字段说明**：
| 字段 | 说明 |
|:---|:---|
| `qid` | 题目全局唯一ID，格式 `Q-{node_id}-{序号}` |
| `source_node_id` | 出题知识点（导入后同时设 `node_id`） |
| `explanation` | 完整解释（含干扰项分析），LLM出题时生成、代码题不留空 |
| `node_id` | 冗余字段，供 Cypher 索引加速 |

题目文件由 `generate_practice_questions.py` 脚本 LLM 批量生成，`validate_data.py` 阶段 1.5 自动校验结构

### 1.4 知识点依赖关系

知识点之间通过 `prerequisites` 字段建立依赖关系。导入 Neo4j 后，这些依赖关系将转化为图谱中的有向边，用于：

- **学情检测Agent**：根据用户掌握节点推断可学习的下一节点
- **图谱管控Agent**：组装渐进式学习路径（确保前置节点排在前面）
- **内容审核Agent**：校验生成内容的逻辑连贯性（不会跳过前置知识点）

### 1.5 Python 方向元知识库（主领域，~100 个节点）

按分类组织，难度呈阶梯分布：

#### 分类一：基础语法（约 20 个节点，难度 1-2）

| ID | 名称 | 难度 | 前置依赖 |
|:---|:---|---:|:---|
| PY-001 | 变量与数据类型 | 1 | 无 |
| PY-002 | 基本输入输出 | 1 | PY-001 |
| PY-003 | 字符串操作 | 1 | PY-001 |
| PY-004 | 算术运算符与表达式 | 1 | PY-001 |
| PY-005 | 比较运算符与布尔逻辑 | 1 | PY-001 |
| PY-006 | if/elif/else 条件判断 | 1 | PY-005 |
| PY-007 | while 循环 | 1 | PY-006 |
| PY-008 | for 循环与 range | 1 | PY-006 |
| PY-009 | 列表(list)基础操作 | 1 | PY-001 |
| PY-010 | 元组(tuple)与集合(set) | 1 | PY-009 |
| PY-011 | 字典(dict)基础操作 | 1 | PY-009 |
| PY-012 | 函数定义与调用 | 2 | PY-006, PY-008 |
| PY-013 | 函数参数（默认/可变/关键字） | 2 | PY-012 |
| PY-014 | 返回值与作用域 | 2 | PY-012 |
| PY-015 | 异常处理 try/except | 2 | PY-006 |
| PY-016 | 文件读写（open/with） | 2 | PY-012, PY-015 |
| PY-017 | 模块与 import | 2 | PY-012 |
| PY-018 | 列表推导式 | 2 | PY-009 |
| PY-019 | 字符串格式化（f-string/format） | 1 | PY-003 |
| PY-020 | 枚举与推导式进阶 | 2 | PY-018 |

#### 分类二：数据结构与算法（约 15 个节点，难度 2-4）

| ID | 名称 | 难度 | 前置依赖 |
|:---|:---|---:|:---|
| PY-021 | 栈与队列 | 2 | PY-009 |
| PY-022 | 链表基础 | 3 | PY-012 |
| PY-023 | 递归思想 | 3 | PY-012 |
| PY-024 | 排序算法（冒泡/选择/插入） | 2 | PY-008, PY-009 |
| PY-025 | 排序算法（快速/归并） | 3 | PY-024 |
| PY-026 | 二分查找 | 2 | PY-006, PY-009 |
| PY-027 | 哈希表与散列 | 3 | PY-011 |
| PY-028 | 树与二叉树 | 3 | PY-022, PY-023 |
| PY-029 | 图的遍历（BFS/DFS） | 4 | PY-028 |
| PY-030 | 动态规划入门 | 4 | PY-023, PY-025 |
| PY-031 | 字符串匹配（KMP） | 4 | PY-003, PY-008 |
| PY-032 | 贪心算法 | 3 | PY-023 |
| PY-033 | 分治策略 | 4 | PY-025 |
| PY-034 | 堆与优先队列 | 3 | PY-009 |
| PY-035 | 并查集 | 4 | PY-028 |

#### 分类三：面向对象编程（约 12 个节点，难度 2-4）

| ID | 名称 | 难度 | 前置依赖 |
|:---|:---|---:|:---|
| PY-036 | 类和对象基础 | 2 | PY-012 |
| PY-037 | `__init__` 与构造方法 | 2 | PY-036 |
| PY-038 | 实例方法与类方法 | 2 | PY-036 |
| PY-039 | 继承与多态 | 3 | PY-036 |
| PY-040 | 封装与 property | 3 | PY-036 |
| PY-041 | 魔术方法 | 3 | PY-036 |
| PY-042 | 抽象类与 ABC | 3 | PY-039 |
| PY-043 | 装饰器基础 | 3 | PY-012 |
| PY-044 | 上下文管理器（contextlib） | 3 | PY-016, PY-043 |
| PY-045 | 迭代器与生成器 | 3 | PY-043 |
| PY-046 | 元类入门 | 4 | PY-041 |
| PY-047 | 设计模式：单例/工厂/观察者 | 3 | PY-039 |

#### 分类四：Python 进阶（约 15 个节点，难度 3-5）

| ID | 名称 | 难度 | 前置依赖 |
|:---|:---|---:|:---|
| PY-048 | 函数式编程（map/filter/reduce） | 3 | PY-012, PY-018 |
| PY-049 | 闭包与作用域 | 3 | PY-043 |
| PY-050 | 高阶函数与 partial | 3 | PY-048 |
| PY-051 | 类型注解（typing） | 3 | PY-012 |
| PY-052 | 正则表达式（re模块） | 3 | PY-003 |
| PY-053 | 日期时间处理（datetime） | 2 | PY-012 |
| PY-054 | JSON与XML解析 | 2 | PY-016 |
| PY-055 | 多线程（threading） | 4 | PY-012, PY-036 |
| PY-056 | 多进程（multiprocessing） | 4 | PY-012 |
| PY-057 | 异步编程（asyncio） | 4 | PY-012, PY-045 |
| PY-058 | 网络请求（requests） | 3 | PY-016 |
| PY-059 | 单元测试（unittest/pytest） | 3 | PY-036 |
| PY-060 | 日志处理（logging） | 2 | PY-012 |
| PY-061 | 配置文件解析（configparser） | 2 | PY-016 |
| PY-062 | 包管理（pip/venv/poetry） | 2 | PY-017 |

#### 分类五：常用库与工具（约 15 个节点，难度 2-4）

| ID | 名称 | 难度 | 前置依赖 |
|:---|:---|---:|:---|
| PY-063 | NumPy 数组基础 | 3 | PY-009 |
| PY-064 | NumPy 矩阵运算 | 3 | PY-063 |
| PY-065 | Pandas Series 与 DataFrame | 3 | PY-063 |
| PY-066 | Pandas 数据清洗 | 3 | PY-065 |
| PY-067 | Matplotlib 基础绘图 | 2 | PY-009 |
| PY-068 | Seaborn 统计可视化 | 3 | PY-067 |
| PY-069 | 爬虫基础（requests+BeautifulSoup） | 3 | PY-016, PY-058 |
| PY-070 | Scrapy 框架入门 | 4 | PY-069 |
| PY-071 | Flask Web 框架基础 | 3 | PY-012 |
| PY-072 | FastAPI 基础 | 3 | PY-012, PY-057 |
| PY-073 | SQLite 数据库操作 | 3 | PY-016 |
| PY-074 | SQLAlchemy ORM 入门 | 3 | PY-073 |
| PY-075 | Git 版本控制基础 | 1 | 无 |
| PY-076 | Docker 基础 | 2 | PY-075 |
| PY-077 | CI/CD 入门（GitHub Actions） | 3 | PY-075, PY-076 |

#### 分类六：项目实战（约 15 个节点，难度 3-5）

| ID | 名称 | 难度 | 前置依赖 |
|:---|:---|---:|:---|
| PY-078 | 命令行工具开发 | 3 | PY-012, PY-017 |
| PY-079 | 配置文件驱动的应用 | 3 | PY-061, PY-078 |
| PY-080 | RESTful API 设计 | 3 | PY-071, PY-072 |
| PY-081 | 数据库 CRUD 应用 | 3 | PY-073, PY-080 |
| PY-082 | 用户认证与 JWT | 4 | PY-080, PY-081 |
| PY-083 | 数据爬虫项目 | 3 | PY-069 |
| PY-084 | 数据清洗与 ETL | 3 | PY-065, PY-066 |
| PY-085 | 数据可视化仪表盘 | 3 | PY-067, PY-068 |
| PY-086 | 机器学习 pipeline | 4 | PY-064, PY-066 |
| PY-087 | Web 应用部署 | 3 | PY-076, PY-080 |
| PY-088 | API 性能优化（缓存/并发） | 4 | PY-055, PY-081 |
| PY-089 | 微服务架构入门 | 4 | PY-072, PY-088 |
| PY-090 | 消息队列（Redis/Celery） | 4 | PY-055, PY-089 |
| PY-091 | 日志监控与告警 | 3 | PY-060, PY-087 |
| PY-092 | 测试覆盖率与质量门禁 | 3 | PY-059, PY-077 |

> 以上仅为核心框架，实际录入时每个节点需补全 `summary`、`key_points`、`practice_questions`、`tags` 字段。以下以 PY-001 和 PY-012 为例展示完整格式：

#### 完整节点示例

**PY-001 变量与数据类型（难度 1）**

```json
{
  "id": "PY-001",
  "name": "变量与数据类型",
  "difficulty": 1,
  "category": "基础语法",
  "summary": "Python 中变量的定义规则与常见数据类型（int、float、str、bool），理解动态类型语言特性",
  "prerequisites": [],
  "key_points": [
    "变量无需声明类型，直接赋值即可",
    "Python 是动态类型语言，变量类型随赋值改变",
    "常见数据类型：int（整数）、float（浮点数）、str（字符串）、bool（布尔值）",
    "type() 函数查看变量类型",
    "命名规则：字母/下划线开头、区分大小写、不能使用关键字"
  ],
  "practice_questions": [
    {
      "type": "choice",
      "question": "以下哪个是正确的 Python 变量名？",
      "options": ["A. 2name", "B. my-name", "C. _count", "D. class"],
      "answer": "C",
      "difficulty": 1,
      "hint": "变量名不能以数字开头，不能包含连字符，不能使用关键字"
    },
    {
      "type": "choice",
      "question": "type(3.14) 的返回结果是什么？",
      "options": ["A. <class 'int'>", "B. <class 'float'>", "C. <class 'str'>", "D. <class 'bool'>"],
      "answer": "B",
      "difficulty": 1
    },
    {
      "type": "fill",
      "question": "在 Python 中，查看变量 x 的数据类型应使用______函数",
      "answer": "type",
      "difficulty": 1
    },
    {
      "type": "code",
      "question": "定义三个变量分别存储你的姓名（字符串）、年龄（整数）和身高（浮点数），然后打印它们",
      "answer": "name = '张三'\nage = 20\nheight = 175.5\nprint(name, age, height)",
      "difficulty": 1
    }
  ],
  "tags": ["基础", "入门", "变量"],
  "estimated_minutes": 20
}
```

**PY-012 函数定义与调用（难度 2）**

```json
{
  "id": "PY-012",
  "name": "函数定义与调用",
  "difficulty": 2,
  "category": "基础语法",
  "summary": "使用 def 关键字定义函数，理解函数的参数传递与返回值机制",
  "prerequisites": ["PY-006", "PY-008"],
  "key_points": [
    "def 关键字定义函数，后跟函数名、参数列表和冒号",
    "函数体需要缩进",
    "return 语句返回值，没有 return 则返回 None",
    "函数必须先定义后调用",
    "参数传递是引用传递（可变对象会受影响）"
  ],
  "practice_questions": [
    {
      "type": "choice",
      "question": "以下函数定义正确的是？",
      "options": ["A. def func():", "B. function func():", "C. define func():", "D. def func()"],
      "answer": "A",
      "difficulty": 1
    },
    {
      "type": "code",
      "question": "编写一个函数 add(a, b)，接收两个参数并返回它们的和",
      "answer": "def add(a, b):\n    return a + b",
      "difficulty": 2
    }
  ],
  "tags": ["基础", "函数"],
  "estimated_minutes": 25
}
```

### 1.6 机器学习方向元知识库（第二领域，~50 个节点）

元知识库从本仓库已有的笔记中提炼，示例如下：

| ID | 名称 | 难度 | 前置依赖 | 来源笔记 |
|:---|:---|---:|:---|---:|
| ML-001 | 人工智能概述 | 1 | 无 | 人工智能与机器学习.md |
| ML-002 | 机器学习分类（监督/无监督/强化） | 1 | ML-001 | 人工智能与机器学习.md |
| ML-003 | 特征与标签 | 1 | ML-002 | 人工智能与机器学习.md |
| ML-004 | 训练集/验证集/测试集划分 | 1 | ML-003 | 人工智能与机器学习.md |
| ML-005 | 线性回归 | 2 | ML-003, PY-064 | 多元线性回归向量化实现.md |
| ML-006 | 多元线性回归 | 2 | ML-005 | 多元线性回归向量化实现.md |
| ML-007 | 特征缩放（标准化/归一化） | 2 | ML-006 | 特征缩放的核心概念整合.md |
| ML-008 | 梯度下降算法 | 3 | ML-006 | 机器学习中的梯度下降算法.md |
| ML-009 | 逻辑回归 | 3 | ML-006, ML-008 | 从零开始认识逻辑回归.md |
| ML-010 | 过拟合与正则化 | 3 | ML-009 | — |
| ... | ... | ... | ... | ... |

### 1.7 数据存储格式

最终产出两种格式，**CSV 用于人工编辑**，**JSON 用于导入 Neo4j**：

#### CSV 格式（人工编辑用）

```csv
id,name,difficulty,category,summary,prerequisites,key_points,practice_questions,tags,estimated_minutes
PY-001,变量与数据类型,1,基础语法,"Python 中变量的定义规则与常见数据类型","[]","[""变量无需声明类型"",""动态类型""]","[{""type"":""choice""}]",基础|入门,20
```

> 注意：CSV 中的 JSON 字段（数组/对象）需转义处理，建议**直接用 JSON 格式编辑**，不要用 CSV。

#### JSON 格式（导入用）

```json
[
  {
    "id": "PY-001",
    "name": "变量与数据类型",
    "difficulty": 1,
    "category": "基础语法",
    ...
  },
  {
    "id": "PY-012",
    "name": "函数定义与调用",
    "difficulty": 2,
    ...
  }
]
```

### 1.8 Neo4j 导入脚本

```python
# scripts/import_knowledge_base.py
"""
将 JSON 格式的领域元知识库导入 Neo4j。
用法: python import_knowledge_base.py --input data/python_knowledge_base.json
"""
import json
import argparse
from neo4j import GraphDatabase


class KnowledgeBaseImporter:
    def __init__(self, uri, user, password):
        self.driver = GraphDatabase.driver(uri, auth=(user, password))

    def close(self):
        self.driver.close()

    def import_nodes(self, nodes: list):
        """批量导入知识节点"""
        with self.driver.session() as session:
            for node in nodes:
                session.run(
                    """
                    MERGE (n:KnowledgeNode {id: $id})
                    SET n.name = $name,
                        n.difficulty = $difficulty,
                        n.category = $category,
                        n.summary = $summary,
                        n.key_points = $key_points,
                        n.practice_questions = $practice_questions,
                        n.common_mistakes = $common_mistakes,
                        n.tags = $tags,
                        n.estimated_minutes = $estimated_minutes
                    """,
                    id=node["id"],
                    name=node["name"],
                    difficulty=node["difficulty"],
                    category=node["category"],
                    summary=node["summary"],
                    key_points=node.get("key_points", []),
                    practice_questions=node.get("practice_questions", []),
                    common_mistakes=node.get("common_mistakes", []),
                    tags=node.get("tags", []),
                    estimated_minutes=node.get("estimated_minutes", 20)
                )

    def import_relationships(self, nodes: list):
        """批量导入前置依赖关系"""
        with self.driver.session() as session:
            for node in nodes:
                prereqs = node.get("prerequisites", [])
                for prereq_id in prereqs:
                    session.run(
                        """
                        MATCH (a:KnowledgeNode {id: $child_id})
                        MATCH (b:KnowledgeNode {id: $parent_id})
                        MERGE (a)-[:REQUIRES]->(b)
                        """,
                        child_id=node["id"],
                        parent_id=prereq_id
                    )

    def run(self, input_file: str):
        with open(input_file, "r", encoding="utf-8") as f:
            nodes = json.load(f)
        print(f"导入 {len(nodes)} 个知识节点...")
        self.import_nodes(nodes)
        print("依赖关系导入中...")
        self.import_relationships(nodes)
        print("完成！")


if __name__ == "__main__":
    parser = argparse.ArgumentParser(description="导入知识库到 Neo4j")
    parser.add_argument("--input", required=True, help="JSON 输入文件路径")
    parser.add_argument("--uri", default="bolt://localhost:7687")
    parser.add_argument("--user", default="neo4j")
    parser.add_argument("--password", default="password")
    args = parser.parse_args()

    importer = KnowledgeBaseImporter(args.uri, args.user, args.password)
    try:
        importer.run(args.input)
    finally:
        importer.close()
```

---

## 二、差异化用户画像

### 2.1 画像字段定义

每份用户画像包含以下字段（v3 格式，2026-06-17 终版）：

| 字段 | 类型 | 说明 | 示例 |
|:---|:---:|:---|---|
| `profile_id` | string | 画像唯一编号 | `UP-BEG-001` |
| `name` | string | 用户称呼（脱敏） | `零基础的小王` |
| `type` | string | 画像类型标签 | `零基础学习者` |
| `description` | string | 画像文字描述 | `完全零编程基础...` |
| `demographics` | object | 人口统计信息 | `{ "education": "大一", "major": "计算机" }` |
| `theory_level` | int | 理论水平 1-5 | `1` |
| `practical_level` | int | 实操水平 1-5 | `1` |
| `learning_style` | string | 学习风格（VARK模型）：visual/auditory/read_write/kinesthetic | `read_write` |
| `target_direction` | string | 学习目标方向（自然语言） | `Python 基础语法入门` |
| `preferred_pace` | string | 学习节奏：slow/normal/fast | `slow` |
| `time_per_week` | int | 每周可投入时间（小时） | `6` |
| `known_topics` | object[] | 已掌握的知识节点及其掌握度 | `[{ "node_id": "PY-001", "mastery": 0.90, "last_test_score": 9.0 }]` |
| `weak_topics` | object[] | 薄弱节点及其典型错误模式 | `[{ "node_id": "PY-012", "mastery": 0.35, "error_patterns": ["切片索引混淆"] }]` |
| `weakness_areas` | string[] | 薄弱环节自然语言描述 | `["对变量和数据类型等基本概念无认知"]` |
| `raw_assessment_data` | object | 原始测评数据记录 | `{ "theory_test": {...}, "practical_test": {...} }` |
| `learning_history` | object[] | 学习历史记录 | `[{ "date": "...", "node_id": "...", "action": "..." }]` |
| `recommended_path` | object | 预计算的学习路径推荐 | `{ "current_node": "PY-001", "next_nodes": [...] }` |

> **v3 格式说明**（2026-06-17 更新）：早期指南使用 `user_id`/`theoretical_level`/`known_node_ids`(string[]) 格式。v3 改为 `profile_id`/`theory_level`/`known_topics`(object[])，增加了 mastery 分数和 error_patterns 维度。`known_topics` 和 `weak_topics` 使用对象数组而非纯 ID 列表，使学情检测 Agent 能基于掌握程度做更精准的推荐。

### 2.2 三组画像详细数据

#### 画像一：零基础组

```json
{
  "profile_id": "UP-BEG-001",
  "name": "零基础的小王",
  "created_at": "2026-07-01T09:00:00Z",
  "type": "零基础学习者",
  "description": "完全零编程基础的大一学生，对编程有好奇心但无任何经验。需要通过最基础的知识节点入门，每一步都需要详细解释。",
  "demographics": {
    "age_range": "18-25",
    "education": "本科在读（大一）",
    "major": "信息管理与信息系统",
    "programming_experience_months": 0,
    "python_experience_months": 0,
    "previously_learned_languages": []
  },
  "theory_level": 1,
  "practical_level": 1,
  "learning_style": "read_write",
  "target_direction": "Python 基础语法入门",
  "preferred_pace": "slow",
  "time_per_week": 6,
  "known_topics": [],
  "weak_topics": [],
  "weakness_areas": [
    "无任何编程经验，对变量、数据类型等基本概念无认知",
    "逻辑思维训练不足，条件判断和循环结构需要从零引导",
    "对编程术语（IDE、编译器、解释器）不熟悉"
  ],
  "raw_assessment_data": {
    "theory_test": { "total_questions": 10, "correct": 2, "answers": [] },
    "practical_test": { "total_exercises": 5, "completed": 0, "average_score": 0 },
    "learning_style_quiz": { "visual": 1, "auditory": 1, "read_write": 4, "kinesthetic": 1 }
  },
  "learning_history": [],
  "recommended_path": {
    "current_node": "PY-001",
    "next_nodes": ["PY-002", "PY-003", "PY-004"],
    "estimated_completion_weeks": 8
  }
}
```

**预期生成的资源特征**：难度 1-2，以实操指南为主，每步有详细解释，练习题以选择题和填空为主，代码题提供脚手架代码。

#### 画像二：入门组

```json
{
  "profile_id": "UP-INT-001",
  "name": "想学爬虫的小李",
  "created_at": "2026-07-01T09:30:00Z",
  "type": "入门学习者",
  "description": "有基本编程概念的大二学生，学过 C 语言，能写简单 Python 脚本但缺乏系统知识。需要补全核心语法和数据结构，目标是做网络爬虫项目。",
  "demographics": {
    "age_range": "18-25",
    "education": "本科在读（大二）",
    "major": "电子信息工程",
    "programming_experience_months": 6,
    "python_experience_months": 2,
    "previously_learned_languages": ["C"]
  },
  "theory_level": 2,
  "practical_level": 2,
  "learning_style": "visual",
  "target_direction": "Python 网络爬虫开发",
  "preferred_pace": "normal",
  "time_per_week": 8,
  "known_topics": [
    {"node_id": "PY-001", "mastery": 0.85, "last_test_score": 8.5},
    {"node_id": "PY-002", "mastery": 0.80, "last_test_score": 8.0},
    {"node_id": "PY-003", "mastery": 0.70, "last_test_score": 7.0}
  ],
  "weak_topics": [
    {"node_id": "PY-012", "mastery": 0.40, "error_patterns": ["列表与元组混淆", "切片负数索引错误"]},
    {"node_id": "PY-020", "mastery": 0.30, "error_patterns": ["字典遍历方式选择不当"]},
    {"node_id": "PY-035", "mastery": 0.25, "error_patterns": ["文件未正确关闭"]}
  ],
  "weakness_areas": [
    "懂基础语法但没做过完整项目，代码组织能力弱",
    "对网络协议、HTTP 请求等了解有限",
    "数据清洗和可视化的实践经验不足"
  ],
  "raw_assessment_data": {
    "theory_test": { "total_questions": 10, "correct": 6, "answers": [] },
    "practical_test": { "total_exercises": 5, "completed": 3, "average_score": 6.5 },
    "learning_style_quiz": { "visual": 3, "auditory": 1, "read_write": 2, "kinesthetic": 1 }
  },
  "learning_history": [],
  "recommended_path": {
    "current_node": "PY-012",
    "next_nodes": ["PY-015", "PY-020", "PY-025"],
    "estimated_completion_weeks": 5
  }
}
```

**预期生成的资源特征**：难度 2-3，以文档+实操为主，跳过基础语法直接进入爬虫相关知识点，侧重从理论到实践的过渡。

#### 画像三：进阶组

```json
{
  "profile_id": "UP-ADV-001",
  "name": "想进阶后端的老赵",
  "created_at": "2026-07-01T10:00:00Z",
  "type": "进阶学习者",
  "description": "有扎实 Python 基础的大三学生，做过课程项目和个人博客，能独立写小型应用。目标是提升代码架构能力和工程素养，学习 FastAPI + Docker 全栈部署。",
  "demographics": {
    "age_range": "18-25",
    "education": "本科在读（大三）",
    "major": "软件工程",
    "programming_experience_months": 18,
    "python_experience_months": 8,
    "previously_learned_languages": ["Java", "JavaScript", "C"]
  },
  "theory_level": 3,
  "practical_level": 4,
  "learning_style": "kinesthetic",
  "target_direction": "Python 后端开发进阶（FastAPI + Docker + 部署）",
  "preferred_pace": "fast",
  "time_per_week": 10,
  "known_topics": [
    {"node_id": "PY-001", "mastery": 0.95, "last_test_score": 9.5},
    {"node_id": "PY-002", "mastery": 0.92, "last_test_score": 9.2},
    {"node_id": "PY-003", "mastery": 0.90, "last_test_score": 9.0},
    {"node_id": "PY-012", "mastery": 0.88, "last_test_score": 8.8},
    {"node_id": "PY-015", "mastery": 0.85, "last_test_score": 8.5},
    {"node_id": "PY-020", "mastery": 0.82, "last_test_score": 8.2}
  ],
  "weak_topics": [
    {"node_id": "PY-050", "mastery": 0.35, "error_patterns": ["装饰器闭包变量作用域错误"]},
    {"node_id": "PY-055", "mastery": 0.30, "error_patterns": ["__init__ vs __new__ 混淆"]},
    {"node_id": "PY-060", "mastery": 0.20, "error_patterns": ["异步编程 await/async 执行顺序理解错误"]}
  ],
  "weakness_areas": [
    "有项目经验但主要用 Java，Python 生态熟悉度不足",
    "对异步编程、高性能 API 设计缺乏经验",
    "Docker 和部署流程不熟悉"
  ],
  "raw_assessment_data": {
    "theory_test": { "total_questions": 10, "correct": 8, "answers": [] },
    "practical_test": { "total_exercises": 5, "completed": 5, "average_score": 8.0 },
    "learning_style_quiz": { "visual": 1, "auditory": 1, "read_write": 2, "kinesthetic": 3 }
  },
  "learning_history": [],
  "recommended_path": {
    "current_node": "PY-050",
    "next_nodes": ["PY-055", "PY-060", "PY-065"],
    "estimated_completion_weeks": 3
  }
}
```

**预期生成的资源特征**：难度 3-4，以文档和挑战任务为主，不包含基础语法内容，聚焦工程实践和架构设计。

### 2.3 画像在系统中的使用流程

```
学情检测Agent
    │
    ├── ① 输入：用户原始数据（问卷/作答/需求）
    │
    ├── ② 处理：比对 known_topics 中每个节点的 mastery 分数
    │       ├── mastery ≥ 0.8 → 标记为绿色（已掌握，跳过）
    │       ├── mastery 0.5-0.8 → 标记为橙色（学习中，可选复习）
    │       ├── mastery < 0.5 → 标记为红色（困难，需重新学习）
    │       └── 不在 known_topics 中的目标节点 → 标记为灰色（未学习）
    │
    ├── ③ 输出：结构化用户画像（JSON）→ 供后续 Agent 读取
    │       └── 画像中的难度水平影响：
    │               ├── 图谱组装时的节点难度筛选
    │               ├── 资源生成时 Prompt 中的"面向对象"设定
    │               └── 阶梯式引导的初始粒度等级
    │
    └── ④ 动态更新：用户答题后 → 更新 known_topics 中对应节点的 mastery
        → 调整 recommended_path → 触发主控调度决策
```

---

## 三、Agent 系统提示词（7个，全部 v0.3-final）

### 3.1 编写规范

每个 Agent 的 Prompt 遵循统一结构：

```
【角色定义】
你是谁，你的专业领域是什么

【核心职责】
你负责做什么，不做什么

【输入格式】
你接收什么数据

【输出格式】
你需要输出什么结构

【约束条件】
你必须遵循的规则（最重要的部分）

【工作流程】
你的执行步骤

【示例】（可选）
输入输出示例
```

### 3.2 学情检测 Agent

```
【角色定义】
你是 KMatch·知链 系统的学情检测 Agent，一名经验丰富的教育测评专家。
你的任务是全面评估学习者的知识水平、实操能力和学习偏好，构建精准的
三维用户能力画像。

【核心职责】
1. 生成理论测评题，覆盖目标领域的基础知识和核心概念
2. 生成实操测评题，检测用户的实际动手能力（代码改错、功能实现）
3. 通过极简问卷判断用户的学习风格偏好
4. 综合所有信息输出结构化的用户画像

【输入格式】
{
  "user_input": {
    "basic_info": {"education": "...", "major": "..."},
    "learning_goal": "用户描述的学习目标",
    "self_assessment": "用户自评",
    "domain": "Python | MachineLearning"
  },
  "knowledge_base": [
    {"id": "PY-001", "name": "...", "difficulty": 1, ...}
  ]
}

【输出格式】
严格按照以下 JSON 格式输出（v3 画像格式）：
{
  "profile_id": "uuid",
  "name": "学习者称呼（脱敏）",
  "theory_level": 1-5,
  "practical_level": 1-5,
  "learning_style": "visual|auditory|read_write|kinesthetic",
  "target_direction": "学习目标方向（自然语言描述）",
  "preferred_pace": "slow|normal|fast",
  "time_per_week": 6,
  "known_topics": [
    {"node_id": "PY-001", "mastery": 0.95, "last_test_score": 9.5}
  ],
  "weak_topics": [
    {"node_id": "PY-012", "mastery": 0.40, "error_patterns": ["切片索引混淆"]}
  ],
  "weakness_areas": [
    "薄弱环节的自然语言描述"
  ],
  "recommended_start_node": "PY-015",
  "created_at": "2026-07-01T10:00:00Z"
}

【约束条件】
1. 理论水平和实操水平必须分开评估，不能混为一谈
2. 如果用户的自评与测评结果矛盾，以测评结果为准
3. 从已知节点向外扩散时，必须考虑前置依赖关系
4. 置信度低于 0.6 时，需要输出 additional_questions 字段补充提问
5. 禁止在输出中添加任何测评过程描述，只输出结构化 JSON（v3 画像格式）

【工作流程】
Step 1: 分析用户输入的基本信息和学习目标
Step 2: 生成 5 道理论选择题（覆盖基础知识、核心概念）
Step 3: 生成 2 道实操题（代码改错或简单功能实现）
Step 4: 提出 2 个学习风格偏好问题
Step 5: 综合评分，输出结构化画像
```

### 3.3 知识图谱管控 Agent

```
【角色定义】
你是 KMatch·知链 系统的知识图谱管控 Agent，一名知识工程专家。
你负责管理和操作 Neo4j 图数据库中的知识图谱，根据不同场景
（有项目/无项目）构建相应的个性化图谱结构。

【核心职责】
1. 无项目场景：根据用户画像从元知识库中抽取节点，组装渐进式学习路径图谱
2. 有项目场景：接收代码解析结果，生成项目框架层+代码实体层图谱
3. 全流程动态更新图谱节点状态（已掌握/学习中/未学习）

【输入格式】
无项目场景：
{
  "scenario": "no_project",
  "user_profile": { ... },  // 学情检测Agent的输出
  "action": "assemble|update_query"
}

有项目场景：
{
  "scenario": "has_project",
  "parsed_code": { ... },  // AST代码解析结果
  "domain_knowledge_base": [ ... ],
  "action": "generate|validate"
}

【输出格式】
{
  "graph_nodes": [
    {"id": "PY-001", "name": "...", "difficulty": 1,
     "status": "mastered|learning|pending", "layer": "meta|project|code|evolve"}
  ],
  "graph_edges": [
    {"source": "PY-001", "target": "PY-012", "relation": "REQUIRES|LEADS_TO|CONTAINS"}
  ],
  "learning_path": ["PY-001", "PY-002", ...],
  "recommendations": {"skip_nodes": [...], "focus_nodes": [...]}
}

【约束条件】
1. 个性化图谱中的每一个节点都必须来自领域元知识库，禁止凭空创建节点
2. 学习路径必须遵循前置依赖关系，前置节点未掌握时不能推荐后置节点
3. 图谱节点状态更新时，级联影响所有下游节点的推荐优先级
4. 有项目场景：代码解析生成的图谱必须与元知识库比对校验，
   不符合规范的依赖关系直接修正
5. 图谱规模控制在 15-25 个节点/路径，避免信息过载

【工作流程】
Step 1: 解析用户画像中的 known_topics 和 target_direction
Step 2: 从 known_topics 中提取 mastery ≥ 0.8 的 node_id 作为种子节点
Step 3: 按依赖关系拓扑排序，生成渐进式学习路径
Step 4: 将 known_topics 中 mastery ≥ 0.8 的节点标记为 mastered
Step 5: 输出图谱结构和学习路径
```

### 3.4 领域知识生成 Agent

```
【角色定义】
你是 KMatch·知链 系统的领域知识生成 Agent，一名资深的计算机教育专家。
你擅长根据知识图谱和用户画像，生成高度个性化、高保真的学习资源。
你**绝不直接给出完整可运行代码**，而是以引导和启发的方式帮助用户学习。

【核心职责】
1. 基于知识图谱和用户画像，生成三种形态的个性化学习资源：
   a) 分层讲义（理论知识讲解）
   b) 阶梯式实操指南（实践操作引导）
   c) 分阶测试题（巩固练习）
2. 二次开发场景：生成阶梯式引导方案，逐步引导用户完成开发
3. 生成的所有内容必须标注所引用的图谱节点ID
4. 支持用户的追问和启发式交互

【输入格式】
{
  "user_profile": { ... },
  "graph_data": { "nodes": [...], "edges": [...], "learning_path": [...] },
  "resource_type": "all|lecture|guide|exercise",
  "current_node_id": "PY-012",  // 当前学习的节点
  "interaction_history": [ ... ],  // 可选，追问场景
  "scenario": "learning|secondary_dev"
}

【输出格式】
{
  "resources": {
    "lecture": {
      "title": "函数定义与调用",
      "content": "...",      // 分层讲解内容（Markdown格式）
      "referenced_nodes": ["PY-012"]
    },
    "guide": {
      "title": "动手实践：编写你的第一个函数",
      "steps": [
        {"step": 1, "description": "...", "hint": "...", "referenced_nodes": ["PY-012"]}
      ],
      "scaffold_level": 1-5  // 当前引导粒度
    },
    "exercises": [
      {
        "type": "choice|fill|code",
        "question": "...",
        "options": ["..."],
        "answer": "...",
        "difficulty": 1-5,
        "referenced_node": "PY-012",
        "hint": "..."
      }
    ]
  },
  "next_suggestions": ["下一阶段建议"],
  "interaction_prompts": ["你可以追问的问题示例"]
}

【约束条件】
1. 每段内容必须标注引用的图谱节点ID，无引用的内容视为无效
2. 绝对不能直接给出完整可运行的代码，只能给方向提示、伪代码或代码片段
3. 实操指南的步骤必须循序渐进，每步说明"为什么这样做"
4. 分阶测试题的难度必须与用户能力水平匹配（偏差不超过 ±1）
5. 二次开发场景的引导粒度遵循 5 级阶梯：
   Level 1: 仅方向提示
   Level 2: 关键知识点 + 技术选型建议
   Level 3: 核心逻辑伪代码
   Level 4: 关键代码片段（非完整实现）
   Level 5: 完整代码 + 逐行解释（仅用户多次尝试失败后解锁）
6. 如果用户追问，优先反问引导用户思考，而非直接给出答案
```

### 3.5 内容审核 Agent

```
【角色定义】
你是 KMatch·知链 系统的内容审核 Agent，一名严格的质量控制专家。
你负责校验知识图谱的正确性和生成内容的专业性，是系统的"质量守门人"。
你只做对错判断，不做内容修改。

【核心职责】
1. 校验知识图谱的节点正确性和依赖关系合理性
2. 校验生成内容与图谱事实的一致性（逐条对比）
3. 幻觉检测与知识溯源标记（标注每句内容对应的图谱节点）
4. 代码审查：检查逻辑错误、代码规范、安全隐患

【输入格式】
{
  "content_type": "graph|lecture|guide|code",
  "content": "... 或 {...}",
  "knowledge_graph": {"nodes": [...], "edges": [...]},
  "domain_knowledge_base": [...]
}

【输出格式】
{
  "verdict": "pass|revision|reject",
  "confidence": 0-1,
  "issues": [
    {
      "severity": "critical|major|minor",
      "type": "hallucination|inconsistency|omission|security",
      "description": "问题描述",
      "referenced_node": "相关图谱节点ID",
      "suggestion": "修改建议（仅文字描述，不包含修改后的代码）"
    }
  ],
  "summary": {
    "total_checks": 12,
    "passed": 10,
    "failed": 2,
    "hallucination_rate": 0.02
  }
}

【约束条件】
1. 所有判断必须以知识图谱中的事实节点为依据，禁止凭空判断
2. 幻觉判定标准：生成内容中的事实性陈述与图谱节点定义不一致
3. critical 级别问题必须有且仅有 1 条明确的问题描述
4. 审核通过的门槛：无 critical 问题，且 hallucination_rate < 5%
5. 如果同一内容连续 3 次审核不通过，标记需主控调度Agent介入裁决
6. 审核Agent只输出问题和判断，不输出修改后的内容

【工作流程】
Step 1: 遍历生成内容中的知识性陈述
Step 2: 对照图谱节点逐条比对验证
Step 3: 标记不一致的内容及其对应的图谱节点
Step 4: 计算幻觉率，汇总问题清单
Step 5: 输出审核结论（pass/revision/reject）
```

### 3.6 代码测试 Agent

```
【角色定义】
你是 KMatch·知链 系统的代码测试 Agent，一名专业的软件测试工程师。
你负责基于知识图谱中的代码节点定义自动生成测试用例，并执行测试。

【核心职责】
1. 基于图谱中的代码实体节点（函数/类/接口）自动生成单元测试用例
2. 执行测试并输出通过率与覆盖率报告
3. 反向标注图谱中的风险节点（未通过测试的模块）

【输入格式】
{
  "code_content": "源代码文本",
  "graph_nodes": [
    {"id": "FN-001", "name": "parse_data", "type": "function",
     "params": ["raw_data: str"], "return": "dict"}
  ],
  "test_type": "unit|integration"
}

【输出格式】
{
  "test_cases": [
    {
      "target": "函数/模块名称",
      "test_code": "生成的测试代码",
      "expected_coverage": 0-1
    }
  ],
  "test_results": {
    "total": 10,
    "passed": 8,
    "failed": 2,
    "coverage": 0.85
  },
  "risk_nodes": [
    {"node_id": "FN-001", "risk": "high|medium|low",
     "reason": "测试未通过"}
  ],
  "suggestions": ["优化建议"]
}

【约束条件】
1. 测试用例必须覆盖正常路径和异常路径
2. 生成的测试代码必须可直接执行（pytest 格式）
3. 风险节点标记后需通知主控调度Agent更新图谱状态
4. 测试Agent仅在代码场景生效，非代码场景不触发
5. 首次测试覆盖率目标 ≥ 60%，优化目标 ≥ 85%
```

### 3.7 主控调度 Agent

```
【角色定义】
你是 KMatch·知链 系统的主控调度 Agent，整个系统的"总指挥官"。
你负责全局流程编排、Agent 间冲突裁决和用户学习状态管理。
所有 Agent 的调用顺序和流转逻辑由你统一控制。

【核心职责】
1. 全局流程调度：决定哪个Agent被调用、按什么顺序调用
2. 冲突裁决：当生成Agent与审核Agent产生分歧时做最终决策
3. 状态管理：维护用户的完整学习进度和系统运行状态
4. 动态迭代决策：根据用户答题正确率决定降维解释/进阶挑战/进入下一节点

【输入格式】
{
  "current_state": {
    "phase": "diagnosis|graph_assembly|content_generation|review|test|feedback",
    "user_id": "...",
    "user_profile": { ... },
    "graph_data": { ... },
    "generated_content": { ... },
    "review_results": { ... },
    "test_results": { ... },
    "iteration_count": 0
  },
  "user_feedback": {
    "exercise_results": { "correct": 3, "total": 5, "accuracy": 0.6 },
    "interaction_log": [...]
  }
}

【输出格式】
{
  "next_action": {
    "type": "call_agent|wait_user|escalate|complete",
    "target_agent": "agent_name",
    "input_data": { ... },
    "reason": "决策理由"
  },
  "state_update": {
    "user_profile_update": { ... },
    "graph_update": { ... },
    "phase": "next_phase"
  },
  "decision": {
    "type": "downgrade|advance|repeat|next_node",
    "reason": "...",
    "details": { ... }
  }
}

【约束条件】
1. 流程必须按"学情检测→图谱管控→内容生成→内容审核→（测试）→反馈迭代"
   的顺序流转，不可跳步
2. 冲突裁决规则：
   - 审核 Agent 判定 revision → 打回生成 Agent 修改，记迭代次数
   - 审核 Agent 判定 reject → 上报主控，主控决定降级或终止
   - 连续 3 次 revision 同一内容 → 主控强制降级处理
3. 动态迭代规则：
   - 答题正确率 ≥ 80% → 进入下一节点或生成进阶挑战
   - 答题正确率 50%-80% → 生成降维解释（换个角度重新讲解）
   - 答题正确率 < 50% → 标记当前节点为"困难"，补充前置基础知识点
4. 所有决策必须有明确的理由记录，不可凭空做决定
```

---

## 四、示例项目代码

### 4.1 项目一：简易爬虫

用于二次开发场景的测试用例。选择一个 **短小精悍、结构清晰** 的开源项目或自行编写。

**推荐来源**：
- GitHub 上搜索 "simple python crawler"，选择 200-500 行的项目
- 自行编写一个包含 3-5 个模块的简易爬虫

**项目结构要求**：

```
simple_crawler/
├── crawler.py          # 核心爬虫逻辑（请求+解析）
├── storage.py          # 数据存储模块
├── config.py           # 配置文件
├── utils.py            # 工具函数
├── tests/
│   ├── test_crawler.py
│   └── test_storage.py
├── requirements.txt
└── README.md
```

### 4.2 项目二：Todo 后端

```
todo_api/
├── app.py              # FastAPI 应用入口
├── models.py           # 数据模型
├── routes/
│   ├── todos.py        # CRUD 路由
│   └── auth.py         # 用户认证路由
├── database.py         # 数据库连接
├── tests/
│   └── test_todos.py
├── requirements.txt
└── README.md
```

---

## 五、代码规范与导入脚本

### 5.1 目录结构与责任分配

```
data/
├── knowledge_base/
│   ├── python_knowledge_base.json    # Python 元知识库 —— A+B+C 按分类编写
│   │   ├── PY-001~020 基础语法          ← B 负责
│   │   ├── PY-021~035 数据结构与算法      ← A 负责
│   │   ├── PY-036~047 面向对象编程        ← C 负责
│   │   ├── PY-048~062 Python 进阶       ← A 负责
│   │   ├── PY-063~077 常用库与工具        ← B 负责
│   │   └── PY-078~092 项目实战           ← C 负责
│   └── ml_knowledge_base.json        # 机器学习元知识库 —— C 负责（约50个节点，复用已有笔记）
├── user_profiles/
│   ├── profile_beginner.json         # 零基础画像 —— B 收集原始数据，C 结构化
│   ├── profile_intermediate.json     # 入门画像
│   └── profile_advanced.json         # 进阶画像
├── prompts/
│   ├── agent_diagnosis.md            # 学情检测 Agent        ← C 编写
│   ├── agent_graph_manager.md        # 知识图谱管控 Agent     ← A 编写
│   ├── agent_content_generator.md    # 领域知识生成 Agent     ← B 编写
│   ├── agent_reviewer.md             # 内容审核 Agent        ← C 编写
│   ├── agent_code_tester.md          # 代码测试 Agent        ← C 编写
│   └── agent_orchestrator.md         # 主控调度 Agent        ← B 编写
└── sample_projects/
    ├── simple_crawler/               # 示例项目一 —— A 准备
    └── todo_api/                     # 示例项目二 —— A 准备

scripts/
├── import_knowledge_base.py          # Neo4j 导入脚本 —— A 编写
└── validate_data.py                  # 数据验证脚本 —— A 编写，C 运行验证
```

### 5.2 数据验证脚本

```python
# scripts/validate_data.py
"""
验证前置数据完整性，确保所有数据符合规范。
"""
import json
import os
import sys


def validate_knowledge_base(filepath):
    """验证元知识库的完整性和正确性"""
    with open(filepath, "r", encoding="utf-8") as f:
        nodes = json.load(f)

    errors = []
    node_ids = set()
    for i, node in enumerate(nodes):
        # 必填字段检查
        for field in ["id", "name", "difficulty", "category", "summary", "key_points"]:
            if field not in node:
                errors.append(f"节点 {i}: 缺少必填字段 {field}")

        # ID 唯一性检查
        if node.get("id") in node_ids:
            errors.append(f"节点 {i}: ID 重复 {node.get('id')}")
        node_ids.add(node.get("id"))

        # 难度范围检查
        if not (1 <= node.get("difficulty", 0) <= 5):
            errors.append(f"节点 {node.get('id')}: difficulty 超出范围 1-5")

        # 前置依赖检查
        for prereq in node.get("prerequisites", []):
            if prereq not in node_ids and prereq not in [n["id"] for n in nodes[:i]]:
                errors.append(f"节点 {node.get('id')}: 前置依赖 {prereq} 不存在")

        # 练习题检查
        questions = node.get("practice_questions", [])
        if not questions:
            errors.append(f"节点 {node.get('id')}: 缺少练习题")
        for q in questions:
            if "type" not in q or "question" not in q:
                errors.append(f"节点 {node.get('id')}: 练习题格式不完整")

    return errors


def validate_user_profile(filepath):
    """验证用户画像的完整性（v3 格式）"""
    with open(filepath, "r", encoding="utf-8") as f:
        profile = json.load(f)

    errors = []
    for field in ["profile_id", "name", "theory_level", "practical_level",
                   "learning_style", "target_direction", "preferred_pace",
                   "time_per_week", "known_topics", "weak_topics", "weakness_areas"]:
        if field not in profile:
            errors.append(f"画像 {filepath}: 缺少字段 {field}")

    # 校验 theory_level / practical_level 范围
    for f in ["theory_level", "practical_level"]:
        val = profile.get(f)
        if val is not None and (not isinstance(val, int) or not (1 <= val <= 5)):
            errors.append(f"画像 {filepath}: {f} 超出范围 1-5")

    # 校验 learning_style 取值
    valid_styles = {"visual", "auditory", "read_write", "kinesthetic"}
    if profile.get("learning_style") not in valid_styles:
        errors.append(f"画像 {filepath}: learning_style 无效，有效值: {valid_styles}")

    # 校验 preferred_pace 取值
    valid_paces = {"slow", "normal", "fast"}
    if profile.get("preferred_pace") not in valid_paces:
        errors.append(f"画像 {filepath}: preferred_pace 无效，有效值: {valid_paces}")

    # 校验 known_topics / weak_topics 结构
    for arr_field in ["known_topics", "weak_topics"]:
        arr = profile.get(arr_field, [])
        if not isinstance(arr, list):
            errors.append(f"画像 {filepath}: {arr_field} 必须是数组")
        else:
            for i, item in enumerate(arr):
                if not isinstance(item, dict) or "node_id" not in item:
                    errors.append(f"画像 {filepath}: {arr_field}[{i}] 缺少 node_id")

    return errors


if __name__ == "__main__":
    data_dir = "data"
    has_error = False

    # 验证元知识库
    kb_dir = os.path.join(data_dir, "knowledge_base")
    for fname in os.listdir(kb_dir):
        if fname.endswith(".json"):
            errors = validate_knowledge_base(os.path.join(kb_dir, fname))
            if errors:
                print(f"❌ {fname}:")
                for e in errors:
                    print(f"   - {e}")
                has_error = True
            else:
                print(f"✅ {fname}: 验证通过")

    # 验证用户画像
    profile_dir = os.path.join(data_dir, "user_profiles")
    for fname in os.listdir(profile_dir):
        if fname.endswith(".json"):
            errors = validate_user_profile(os.path.join(profile_dir, fname))
            if errors:
                print(f"❌ {fname}:")
                for e in errors:
                    print(f"   - {e}")
                has_error = True
            else:
                print(f"✅ {fname}: 验证通过")

    sys.exit(1 if has_error else 0)
```

---

## 附录：数据质量检查清单

### 元知识库

- [ ] 所有节点 ID 唯一，无重复
- [ ] 所有难度值在 1-5 范围内
- [ ] 所有前置依赖对应的节点 ID 真实存在（无悬空引用）
- [ ] 依赖关系无循环引用（如 A→B→C→A）
- [ ] 每个节点至少包含 1 道练习题
- [ ] 每个节点至少包含 1 条 `common_mistakes`（常见误区，schema.json minItems=1）
- [ ] 练习题的答案标注正确
- [ ] JSON 格式合法，可通过 `json.load()` 解析
- [ ] Neo4j 导入脚本执行无报错
- [ ] `questions/` 独立题目库 92 文件全部通过 `validate_data.py` 阶段 1.5 校验
- [ ] Python 方向 ≥ 80 个节点，机器学习方向 ≥ 40 个节点

### 用户画像

- [ ] 3 组画像的 `theory_level` 覆盖低/中/高（1-2/3/4-5）
- [ ] 每组画像的 `known_topics` 与实际水平匹配（零基础 = 空数组）
- [ ] 每组画像的 `weak_topics` 包含至少 2 个薄弱节点及其 error_patterns
- [ ] `learning_style` 覆盖至少两种风格（VARK 模型）
- [ ] `preferred_pace` 设置合理（零基础=slow / 入门=normal / 进阶=fast）
- [ ] JSON 格式合法，通过 `validate_data.py` 校验

### Prompt

- [ ] 7 个 Agent 都有完整的角色定义、职责、输入输出格式、约束条件
- [ ] "内容审核Agent"和"代码审查Agent"的职责边界明确（05审内容、07审代码）
- [ ] "内容审核Agent"和"代码测试Agent"的职责边界明确（审核只校验，测试才执行）
- [ ] 生成Agent 明确禁止输出完整可运行代码
- [ ] 主控调度Agent 定义了迭代次数上限和冲突裁决规则
- [ ] Prompt 中的字段名与后端代码中的字段名一致

### 示例项目

- [ ] 项目代码可正常运行（`pip install -r requirements.txt && python app.py`）
- [ ] 项目包含测试目录和至少 2 个测试用例
- [ ] 项目代码量适中（100-500 行），结构清晰，适合作为二次开发引导素材

### M5 质量检测（赛题要求）

- [ ] 幻觉率 < 5%（`run_quality_test.py` 批量跑 3 画像）
- [ ] 适配率 ≥ 85%（资源 difficulty_level 与知识点难度 gap=0，系统赋值非 LLM 臆造）
- [ ] 覆盖率 ≥ 90%（弱项节点全部纳入学习路径，BUG-042 修复）
- [ ] 质量报告已生成（`data/quality_report.json` + `docs/质量检测报告.md`）

---

*文档版本：V2.0*
*最后更新：2026年6月19日*
*变更摘要：新增 common_mistakes 字段说明 + questions/ 独立题目库文档 + 7个 Prompt 全部 v0.3-final + M5 质量检测指标；Cypher 示例补 common_mistakes；全部数据通过 validate_data v3 三阶段校验。*
*协调人：C（数据与质量负责人）*
*参与成员：A（后端）+ B（前端）+ C（数据）*
